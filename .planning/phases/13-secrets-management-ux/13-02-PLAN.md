---
phase: 13-secrets-management-ux
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - apps/web/app/(dashboard)/businesses/[id]/settings/page.tsx
  - apps/web/_components/settings-page.tsx
  - apps/web/_components/secrets-manager.tsx
  - apps/web/_components/provider-secrets-card.tsx
  - apps/web/_components/credential-form.tsx
  - apps/web/_components/sidebar-nav.tsx
  - apps/web/_components/health-dashboard.tsx
  - apps/web/app/(dashboard)/businesses/[id]/settings/secrets/page.tsx
autonomous: true
requirements:
  - SECR-ENH-02
  - SECR-ENH-04

must_haves:
  truths:
    - "Settings page exists at /businesses/[id]/settings with Emergency Controls and Secrets sections"
    - "Settings page is accessible from sidebar nav and gear icon on Overview page"
    - "Secrets are grouped by provider with collapsible cards showing connection status"
    - "Eye toggle reveals the actual decrypted secret value for 5 seconds then re-masks"
    - "Edit in place allows updating a credential field and saving the new encrypted value"
    - "Deleting a provider's credentials deactivates its integration record"
    - "Only configured providers (with saved credentials) appear on the Secrets section"
  artifacts:
    - path: "apps/web/app/(dashboard)/businesses/[id]/settings/page.tsx"
      provides: "Settings page Server Component"
      min_lines: 30
    - path: "apps/web/_components/settings-page.tsx"
      provides: "Settings page client component with Emergency Controls and Secrets sections"
      min_lines: 50
    - path: "apps/web/_components/secrets-manager.tsx"
      provides: "Overhauled secrets manager grouped by provider"
      min_lines: 40
    - path: "apps/web/_components/provider-secrets-card.tsx"
      provides: "Collapsible provider card with credential fields, eye toggle, edit, delete"
      min_lines: 60
    - path: "apps/web/_components/credential-form.tsx"
      provides: "Dynamic credential form driven by provider field definitions"
      min_lines: 40
    - path: "apps/web/_components/sidebar-nav.tsx"
      provides: "Updated sidebar with Settings link"
      contains: "Settings"
  key_links:
    - from: "apps/web/_components/settings-page.tsx"
      to: "apps/web/_components/secrets-manager.tsx"
      via: "renders SecretsManager in Secrets section"
      pattern: "<SecretsManager"
    - from: "apps/web/_components/provider-secrets-card.tsx"
      to: "apps/web/_actions/secrets-actions.ts"
      via: "calls revealSecretAction, testConnectionAction, deleteProviderSecretsAction"
      pattern: "revealSecretAction|testConnectionAction|deleteProviderSecretsAction"
    - from: "apps/web/_components/health-dashboard.tsx"
      to: "apps/web/app/(dashboard)/businesses/[id]/settings/page.tsx"
      via: "gear icon links to /settings"
      pattern: "settings"
    - from: "apps/web/_components/sidebar-nav.tsx"
      to: "apps/web/app/(dashboard)/businesses/[id]/settings/page.tsx"
      via: "Settings nav link"
      pattern: "settings"
---

<objective>
Settings page with Emergency Controls and Secrets sections, overhauled provider-grouped secrets UI with collapsible cards, eye toggle reveal, edit-in-place, and updated navigation.

Purpose: Replaces the gear dropdown on Overview with a full Settings page. Transforms the secrets UX from flat category-based listing to provider-grouped collapsible cards with real decryption reveal, edit-in-place updates, and test connection. Secrets page is now a section on the Settings page, accessible from both sidebar and gear icon.

Output: Settings page, overhauled SecretsManager, ProviderSecretsCard, CredentialForm, updated sidebar nav, updated health dashboard gear icon.
</objective>

<execution_context>
@/Users/tjbrooks/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tjbrooks/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-secrets-management-ux/13-RESEARCH.md
@.planning/phases/13-secrets-management-ux/13-01-SUMMARY.md
@apps/web/_components/health-dashboard.tsx
@apps/web/_components/emergency-controls.tsx
@apps/web/_components/sidebar-nav.tsx
@apps/web/_components/secrets-manager.tsx
@apps/web/app/(dashboard)/businesses/[id]/settings/secrets/page.tsx
@apps/web/_actions/secrets-actions.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Settings page with Emergency Controls section and navigation updates</name>
  <files>
    apps/web/app/(dashboard)/businesses/[id]/settings/page.tsx
    apps/web/_components/settings-page.tsx
    apps/web/_components/sidebar-nav.tsx
    apps/web/_components/health-dashboard.tsx
    apps/web/app/(dashboard)/businesses/[id]/settings/secrets/page.tsx
  </files>
  <action>
    Create apps/web/app/(dashboard)/businesses/[id]/settings/page.tsx (Server Component):
    - Auth check with redirect
    - Fetch business by id (notFound if missing)
    - Fetch secrets grouped by provider via getSecretsByProviderAction
    - Fetch all provider field definitions via getAllProviderFieldsAction
    - Render SettingsPage client component with business, grouped secrets, and field definitions as props

    Create apps/web/_components/settings-page.tsx ("use client"):
    - Single scrollable page with two clearly separated sections using anchor ids
    - Section 1: "Emergency Controls" (id="emergency-controls")
      - Card with title "Emergency Controls"
      - Contains the tenant-level emergency controls currently in the health dashboard dropdown: Disable Tenant (with TypeToConfirmDialog) and Restore Tenant
      - Import disableTenantAction and restoreTenantAction from emergency-actions
      - Show business status badge, disable/restore button based on status
    - Section 2: "Secrets & Credentials" (id="secrets")
      - Renders the overhauled SecretsManager component (built in Task 2)
      - Passes grouped secrets and provider field definitions
    - Clean section headers with icons (Shield for Emergency Controls, Lock for Secrets)

    Update apps/web/_components/sidebar-nav.tsx:
    - Add Settings link to getBusinessSubNav between "Knowledge Base" and "Logs":
      { href: `/businesses/${businessId}/settings`, label: "Settings", icon: Settings, enabled: true }
    - Import Settings icon from lucide-react

    Update apps/web/_components/health-dashboard.tsx:
    - Replace the DropdownMenu (Settings gear dropdown with Emergency Controls) with a simple Link/Button that navigates to `/businesses/${businessId}/settings`
    - Keep the gear icon and "Settings" label, but make it a Link instead of a dropdown trigger
    - Remove the DropdownMenuContent with Emergency Controls items (they move to the Settings page)
    - Remove the TypeToConfirmDialog for disable tenant (it moves to the Settings page)
    - Remove the related state (showDisableTenant, isPending for disable/restore) and handler functions (handleDisableTenant, handleRestoreTenant) that were only used by the dropdown
    - Keep the VPS warning banner and all other dashboard content unchanged

    Update apps/web/app/(dashboard)/businesses/[id]/settings/secrets/page.tsx:
    - Change it to redirect to `/businesses/${businessId}/settings#secrets` so old bookmarks still work
    - Simple redirect, no content rendering
  </action>
  <verify>
    Run `pnpm turbo typecheck` -- must pass.
    Verify the sidebar nav shows "Settings" link.
    Verify /settings/secrets redirects to /settings#secrets.
  </verify>
  <done>
    Settings page exists at /businesses/[id]/settings with Emergency Controls and Secrets sections.
    Sidebar nav has Settings link.
    Gear icon on Overview links to Settings page instead of opening a dropdown.
    Old /settings/secrets URL redirects to new Settings page.
  </done>
</task>

<task type="auto">
  <name>Task 2: Provider-grouped secrets manager with collapsible cards, eye toggle, and credential form</name>
  <files>
    apps/web/_components/secrets-manager.tsx
    apps/web/_components/provider-secrets-card.tsx
    apps/web/_components/credential-form.tsx
  </files>
  <action>
    Overhaul apps/web/_components/secrets-manager.tsx ("use client"):
    - Accept props: groupedSecrets (Record<string, Secret[]>), providerFields (Record<string, CredentialField[]>), businessId
    - Render only configured providers (providers that have at least one saved credential) per user decision
    - For each provider group, render a ProviderSecretsCard component
    - Show legacy/uncategorized secrets (provider = null) at the bottom in a separate "Legacy Credentials" section with the old flat display style
    - Empty state when no secrets at all: "No credentials configured. Add integrations from the Integrations page to set up credentials."
    - Remove the old "Add Secret" form (adding new providers is done from the Integrations page per user decision -- no "Add Integration" button on Secrets page)
    - Remove old category-based grouping logic

    Create apps/web/_components/provider-secrets-card.tsx ("use client"):
    - Props: provider (string), secrets (Secret[]), fields (CredentialField[]), businessId
    - Collapsible card using Collapsible from shadcn/ui:
      - Collapsed: provider display name (from INTEGRATION_CATALOG lookup), connection status badge (green "connected" if secrets exist, or use integration status), last updated date from most recent secret
      - Provider logo: render img with src from INTEGRATION_CATALOG logoUrl (e.g., /integrations/hubspot.svg)
      - Expanded: CredentialForm for editing existing values + action buttons
    - Eye toggle per field:
      - Click eye icon -> call revealSecretAction(businessId, secretId) server action
      - Show loading spinner while decrypting
      - Display plaintext for 5 seconds (setTimeout), then auto-re-mask
      - Cache revealed value client-side during the 5-second window so re-toggling doesn't hit server again
    - "Test Connection" button:
      - Calls testConnectionAction(businessId, provider)
      - Shows success/failure toast with message
      - Disabled until credentials are saved
    - "Delete All Credentials" button:
      - Calls deleteProviderSecretsAction(businessId, provider)
      - Confirmation via AlertDialog before deleting
      - Shows toast on success/error
      - Per user decision: deleting credentials deactivates the integration record
    - "Save Changes" button for edit-in-place:
      - Calls saveProviderCredentialsAction(businessId, provider, updatedValues)
      - Only sends fields that were actually changed
      - Shows toast on success/error

    Create apps/web/_components/credential-form.tsx ("use client"):
    - Props: fields (CredentialField[]), existingSecrets (Secret[]), onSave (async callback with Record<string, string>), isSaving (boolean)
    - Renders one Input per field definition, ordered by field_order
    - Input type maps from field_type: 'password' -> type="password", 'text' -> type="text", 'url' -> type="url"
    - Label from display_label, placeholder from placeholder, help text from help_text (rendered as small text below input)
    - All fields shown for a provider are required (per user decision)
    - For existing secrets: show masked placeholder ("****" or actual revealed value if eye toggle active)
    - Track which fields have been modified via local state
    - Submit button labeled "Save Changes" or "Save Credentials" depending on whether editing or new entry
    - Show last updated date per field if available (from the matching secret's updated_at)
  </action>
  <verify>
    Run `pnpm turbo typecheck` -- must pass.
    Verify provider-secrets-card uses Collapsible pattern.
    Verify credential-form renders inputs dynamically from field definitions.
    Verify eye toggle has 5-second auto-re-mask setTimeout.
  </verify>
  <done>
    SecretsManager groups by provider with collapsible ProviderSecretsCard components.
    Eye toggle reveals decrypted value for 5 seconds then auto-re-masks.
    CredentialForm renders dynamic fields from provider_credential_fields DB data.
    Edit-in-place, test connection, and delete-all work through server actions.
    Only configured providers shown. Legacy secrets shown separately at the bottom.
  </done>
</task>

</tasks>

<verification>
- `pnpm turbo typecheck` passes
- Settings page at /businesses/[id]/settings renders two sections: Emergency Controls and Secrets
- Sidebar nav shows Settings link between Knowledge Base and Logs
- Gear icon on Overview page navigates to /settings (no longer a dropdown)
- /settings/secrets redirects to /settings#secrets
- Secrets grouped by provider with collapsible cards
- Eye toggle decrypts and reveals for 5 seconds
- Test Connection button returns mock success
- Delete All Credentials deactivates integration
- Edit-in-place saves updated credentials
- Legacy secrets with null provider shown in separate section
</verification>

<success_criteria>
- Settings page exists and is navigable from sidebar and gear icon
- Emergency Controls moved from dropdown to Settings page section
- Secrets display uses provider-grouped collapsible cards
- Eye toggle, test connection, edit-in-place, and delete all functional
- Old /settings/secrets URL redirects to new location
</success_criteria>

<output>
After completion, create `.planning/phases/13-secrets-management-ux/13-02-SUMMARY.md`
</output>
