---
phase: 13-secrets-management-ux
plan: 03
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - apps/web/_components/credential-side-drawer.tsx
  - apps/web/_components/integration-config-card.tsx
  - apps/web/_components/integrations-overview.tsx
  - apps/web/app/(dashboard)/businesses/[id]/integrations/page.tsx
autonomous: true
requirements:
  - SECR-ENH-01
  - SECR-ENH-03

must_haves:
  truths:
    - "Clicking Configure on an integration card opens a side drawer with the credential form"
    - "The side drawer shows dynamic credential fields based on the integration provider"
    - "Saving credentials from the side drawer auto-creates an integration record if needed"
    - "The side drawer closes and integration status updates after successful save"
    - "Category auto-fills based on the selected integration provider"
  artifacts:
    - path: "apps/web/_components/credential-side-drawer.tsx"
      provides: "Right-side drawer with dynamic credential form for integration configuration"
      min_lines: 60
    - path: "apps/web/_components/integration-config-card.tsx"
      provides: "Updated card with Configure button that opens side drawer"
      contains: "Configure"
    - path: "apps/web/_components/integrations-overview.tsx"
      provides: "Updated overview that manages drawer state"
      contains: "CredentialSideDrawer"
  key_links:
    - from: "apps/web/_components/integration-config-card.tsx"
      to: "apps/web/_components/credential-side-drawer.tsx"
      via: "Configure button onClick opens drawer"
      pattern: "onConfigure|setDrawerProvider"
    - from: "apps/web/_components/credential-side-drawer.tsx"
      to: "apps/web/_actions/secrets-actions.ts"
      via: "calls saveProviderCredentialsAction, getProviderFieldsAction"
      pattern: "saveProviderCredentialsAction|getProviderFieldsAction"
---

<objective>
Credential side drawer on the Integrations page that opens when clicking "Configure" on an integration card, showing dynamic credential fields driven by provider definitions from the database.

Purpose: Implements the seamless integration-first credential flow from the Integrations page. Users click Configure -> drawer opens -> fill credentials -> save -> integration activated. No page navigation required. This is one of the two entry points for credential management (the other being the Settings/Secrets page built in Plan 02).

Output: CredentialSideDrawer component, updated IntegrationConfigCard with Configure button, updated integrations page data flow.
</objective>

<execution_context>
@/Users/tjbrooks/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tjbrooks/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-secrets-management-ux/13-RESEARCH.md
@.planning/phases/13-secrets-management-ux/13-01-SUMMARY.md
@apps/web/_components/integration-config-card.tsx
@apps/web/_components/integrations-overview.tsx
@apps/web/app/(dashboard)/businesses/[id]/integrations/page.tsx
@apps/web/_actions/secrets-actions.ts
@apps/web/_components/task-detail-panel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Credential side drawer component</name>
  <files>
    apps/web/_components/credential-side-drawer.tsx
  </files>
  <action>
    Create apps/web/_components/credential-side-drawer.tsx ("use client"):
    - Follow the existing fixed-position side panel pattern from task-detail-panel.tsx and profile-editor-drawer.tsx:
      - Fixed positioned div: right-0, z-50, w-[420px], border-l, bg-background, shadow-xl
      - Slide-in/out transition: translate-x-0 when open, translate-x-full when closed
      - Backdrop overlay: fixed inset-0 z-40 bg-black/20 that closes drawer on click
    - Props: isOpen (boolean), onClose (callback), provider (string | null), businessId (string)
    - When provider is set and drawer opens:
      - Fetch provider field definitions via getProviderFieldsAction(provider) on mount/provider change
      - Fetch existing secrets for this provider via getSecretsByProviderAction(businessId) and filter to the provider
      - Show loading skeleton while fetching
    - Header:
      - Provider logo (from INTEGRATION_CATALOG logoUrl)
      - Provider name and description (from INTEGRATION_CATALOG)
      - Close button (X icon)
    - Body: Render CredentialForm (from credential-form.tsx created in Plan 02) with fetched field definitions and existing secrets
      - All fields required per user decision
      - If credentials already exist, pre-fill with masked values and allow editing
    - Footer:
      - "Save Credentials" / "Update Credentials" button
      - "Test Connection" button (enabled after save)
      - Cancel button
    - On save:
      - Call saveProviderCredentialsAction(businessId, provider, credentials)
      - Show success toast: "Credentials saved for {providerName}"
      - If this is a new configuration (no prior credentials), toast: "Credentials saved and {providerName} integration activated"
      - Call onClose() after successful save
      - The server action auto-creates the integration record (handled in Plan 01)
    - On test connection:
      - Call testConnectionAction(businessId, provider)
      - Show result as toast (success green, failure red)
    - Keyboard: Escape key closes drawer

    NOTE: This component imports CredentialForm from credential-form.tsx. If Plan 02 runs in parallel and hasn't created it yet, create a minimal version inline that renders Input fields from the field definitions array. Keep the same interface so it can be swapped when Plan 02 lands.
  </action>
  <verify>
    Run `pnpm turbo typecheck` -- must pass.
    Verify drawer has fixed positioning, z-50, slide transition pattern.
    Verify it fetches field definitions and renders dynamic form.
  </verify>
  <done>
    CredentialSideDrawer renders a right-side sliding panel with dynamic credential form.
    Provider field definitions drive the form fields.
    Save action stores encrypted credentials and auto-creates integration.
    Test connection returns mock result via toast.
  </done>
</task>

<task type="auto">
  <name>Task 2: Configure button on integration cards and drawer integration</name>
  <files>
    apps/web/_components/integration-config-card.tsx
    apps/web/_components/integrations-overview.tsx
    apps/web/app/(dashboard)/businesses/[id]/integrations/page.tsx
  </files>
  <action>
    Update apps/web/_components/integration-config-card.tsx:
    - Add a "Configure" button next to the existing "View Setup" button in the card header
    - Use a wrench or Settings icon from lucide-react (Settings2 or Wrench)
    - Configure button calls an onConfigure(provider) callback prop
    - Props interface: add onConfigure: (provider: string) => void
    - The Configure button should be subtle (ghost variant, small size) matching the "View Setup" button style

    Update apps/web/_components/integrations-overview.tsx:
    - Add state for the credential drawer: drawerProvider (string | null), isDrawerOpen (boolean)
    - Pass onConfigure={(provider) => { setDrawerProvider(provider); setIsDrawerOpen(true); }} to each IntegrationConfigCard
    - Render CredentialSideDrawer at the bottom of the component with isOpen, onClose, provider, businessId props
    - onClose: setIsDrawerOpen(false), setDrawerProvider(null), call router.refresh() to update integration statuses
    - Import CredentialSideDrawer from credential-side-drawer.tsx

    Update apps/web/app/(dashboard)/businesses/[id]/integrations/page.tsx:
    - No changes needed if IntegrationsOverview already receives businessId
    - Verify the businessId prop flows through to IntegrationsOverview -> IntegrationConfigCard -> CredentialSideDrawer
  </action>
  <verify>
    Run `pnpm turbo typecheck` -- must pass.
    Verify IntegrationConfigCard has "Configure" button with onConfigure callback.
    Verify IntegrationsOverview manages drawer state and renders CredentialSideDrawer.
  </verify>
  <done>
    Each integration card has a "Configure" button that opens the credential side drawer.
    Side drawer shows dynamic credential form for the selected provider.
    Saving credentials from the drawer activates the integration.
    Integration statuses refresh after drawer closes.
  </done>
</task>

</tasks>

<verification>
- `pnpm turbo typecheck` passes
- Integration cards show "Configure" button alongside "View Setup"
- Clicking "Configure" opens right-side drawer with provider-specific credential fields
- Drawer fetches field definitions from DB and renders dynamic form
- Saving credentials encrypts values, stores in secrets table, auto-creates integration record
- Test Connection button shows mock success toast
- Drawer closes on save, Escape key, backdrop click, or X button
- Integration status updates after credential save (refreshes page data)
</verification>

<success_criteria>
- Credential side drawer opens from integration cards
- Dynamic form renders correct fields per provider
- Save flow encrypts, stores, and activates integration
- Seamless UX: Configure -> fill -> save -> done (no page navigation)
</success_criteria>

<output>
After completion, create `.planning/phases/13-secrets-management-ux/13-03-SUMMARY.md`
</output>
