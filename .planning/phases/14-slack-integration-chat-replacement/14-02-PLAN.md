---
phase: 14-slack-integration-chat-replacement
plan: 02
type: execute
wave: 2
depends_on:
  - 14-01
files_modified:
  - apps/web/app/(dashboard)/businesses/[id]/integrations/page.tsx
  - apps/web/_components/slack-connect-card.tsx
  - apps/web/app/(dashboard)/businesses/[id]/chat/page.tsx
  - apps/web/_components/chat-layout.tsx
  - apps/web/_components/chat-channel-list.tsx
  - apps/web/_components/chat-message-list.tsx
  - apps/web/_components/chat-message-bubble.tsx
  - apps/web/_components/chat-message-input.tsx
  - apps/web/_components/slack-connect-prompt.tsx
  - apps/web/_components/slack-channel-header.tsx
  - apps/web/_actions/chat-actions.ts
autonomous: true
requirements:
  - SLACK-02
  - SLACK-04

must_haves:
  truths:
    - "Integrations page has a Connect Slack card/section as the primary entry point for Slack OAuth (per user decision)"
    - "When Slack is NOT connected, chat page shows a secondary Connect Slack CTA that directs users to the integrations page"
    - "When Slack IS connected, chat page displays Slack-synced messages from the Supabase messages table"
    - "Channel sidebar shows Slack channels mapped to departments instead of generic department list"
    - "Channel header displays Connected to Slack status badge with Open in Slack deep link button"
    - "Messages display bidirectionally: messages sent from admin panel appear in Slack, Slack messages appear in admin panel"
    - "Admin can send messages from the chat page that are posted to the mapped Slack channel"
    - "Custom chat infrastructure is replaced: no fallback to old stub-based chat when Slack is connected"
  artifacts:
    - path: "apps/web/_components/slack-connect-card.tsx"
      provides: "Slack connection card for the integrations page with Connect/Disconnect button and OAuth popup"
      contains: "Connect Slack"
    - path: "apps/web/app/(dashboard)/businesses/[id]/integrations/page.tsx"
      provides: "Updated integrations page that renders the Slack connect card above the integrations overview"
      contains: "SlackConnectCard"
    - path: "apps/web/_components/slack-connect-prompt.tsx"
      provides: "Secondary CTA on chat page directing users to integrations page when Slack is not connected"
      contains: "integrations"
    - path: "apps/web/_components/slack-channel-header.tsx"
      provides: "Channel header with Slack connection badge and Open in Slack deep link"
      contains: "Open in Slack"
    - path: "apps/web/_components/chat-layout.tsx"
      provides: "Refactored chat layout that conditionally shows Slack-connected or Connect prompt"
      contains: "slackStatus"
    - path: "apps/web/_components/chat-channel-list.tsx"
      provides: "Updated channel sidebar showing Slack channel names with hash prefix"
      contains: "slack_channel_name"
    - path: "apps/web/_actions/chat-actions.ts"
      provides: "Updated chat actions with Slack message send integration"
      exports: ["sendSlackMessageAction"]
  key_links:
    - from: "apps/web/app/(dashboard)/businesses/[id]/integrations/page.tsx"
      to: "apps/web/_components/slack-connect-card.tsx"
      via: "renders Slack connect card as primary OAuth entry point"
      pattern: "import.*SlackConnectCard"
    - from: "apps/web/_components/slack-connect-card.tsx"
      to: "apps/web/_actions/slack-actions.ts"
      via: "calls connectSlackAction to get OAuth URL and getSlackStatusAction for status"
      pattern: "import.*from.*slack-actions"
    - from: "apps/web/app/(dashboard)/businesses/[id]/chat/page.tsx"
      to: "apps/web/_actions/slack-actions.ts"
      via: "import getSlackStatusAction to determine connected state"
      pattern: "import.*getSlackStatusAction.*from.*slack-actions"
    - from: "apps/web/_components/chat-layout.tsx"
      to: "apps/web/_components/slack-connect-prompt.tsx"
      via: "render when Slack is not connected"
      pattern: "import.*SlackConnectPrompt"
    - from: "apps/web/_actions/chat-actions.ts"
      to: "packages/core/slack/slack-messages.ts"
      via: "import postAgentResponseToSlack for outbound messages"
      pattern: "import.*from.*@fleet-factory/core"
---

<objective>
Replace the custom chat page with a Slack-powered interface. When Slack is connected, show Slack-synced messages with channel-based navigation, bidirectional messaging, and "Open in Slack" deep links. When not connected, show a "Connect Slack" call-to-action.

Purpose: The chat page transitions from a stub/VPS-based chat to a Slack-first experience. The admin panel becomes a full participant in Slack conversations -- sending messages that appear in Slack, and displaying Slack messages in the panel. No duplicate chat infrastructure remains when Slack is connected.

Output: Updated chat page, refactored layout and components, new Connect Slack prompt, updated channel header, and updated server actions.
</objective>

<execution_context>
@/Users/tjbrooks/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tjbrooks/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-slack-integration-chat-replacement/14-RESEARCH.md
@.planning/phases/14-slack-integration-chat-replacement/14-CONTEXT.md
@.planning/phases/14-slack-integration-chat-replacement/14-01-PLAN.md
@apps/web/app/(dashboard)/businesses/[id]/chat/page.tsx
@apps/web/_components/chat-layout.tsx
@apps/web/_components/chat-channel-list.tsx
@apps/web/_components/chat-message-list.tsx
@apps/web/_components/chat-message-bubble.tsx
@apps/web/_components/chat-message-input.tsx
@apps/web/_actions/chat-actions.ts
@apps/web/_actions/slack-actions.ts
@packages/core/slack/slack-types.ts
@packages/core/chat/chat-types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrations page Slack connect card (primary OAuth entry point)</name>
  <files>
    apps/web/_components/slack-connect-card.tsx
    apps/web/app/(dashboard)/businesses/[id]/integrations/page.tsx
  </files>
  <action>
    Per user decision: "Connect Slack lives on the integrations page (Phase 12 catalog) -- not a separate settings section." This task adds the primary Connect Slack entry point to the integrations page.

    **Create apps/web/_components/slack-connect-card.tsx:**
    Client component. A prominent card shown on the integrations page for connecting/managing the Slack workspace integration.

    When Slack is NOT connected:
    - Card with Slack logo/icon (use MessageSquare icon or inline Slack SVG), heading "Slack", description "Connect your Slack workspace to route messages between department agents and Slack channels"
    - "Connect Slack" primary button that opens the OAuth install URL in a popup window (window.open with centered popup dimensions ~600x700)
    - After popup closes, poll getSlackStatusAction every 2 seconds for up to 30 seconds to detect successful connection, then reload the page via router.refresh()
    - Badge: "Not connected"

    When Slack IS connected:
    - Card shows workspace name (from slackStatus.teamName), "Connected" badge (green)
    - Show channel count (number of mapped channels)
    - "Open Slack Workspace" link (opens https://slack.com/app_redirect?team={teamId})
    - "Disconnect" button (calls disconnectSlackAction, with confirmation dialog)
    - "Manage Channels" link to /businesses/{businessId}/chat (where channel mappings are visible)

    Props: businessId (string), initialStatus (SlackConnectionStatus)

    Import connectSlackAction, getSlackStatusAction, disconnectSlackAction from slack-actions.

    **Update apps/web/app/(dashboard)/businesses/[id]/integrations/page.tsx:**
    - Import SlackConnectCard from _components/slack-connect-card
    - Import getSlackStatusAction from _actions/slack-actions
    - Fetch Slack status via getSlackStatusAction(businessId) alongside existing data
    - Render SlackConnectCard ABOVE the IntegrationsOverview component, within a section labeled "Workspace Integrations" or similar, to visually separate the workspace-level Slack connection from the per-agent/department integration cards
    - Pass businessId and initialStatus to SlackConnectCard
  </action>
  <verify>
    Run `pnpm turbo typecheck` -- should pass.
    Verify integrations page renders SlackConnectCard.
    Verify SlackConnectCard shows connect/disconnect state.
  </verify>
  <done>
    Integrations page has a Slack connect card as the primary entry point for OAuth (per user decision).
    Card shows connect button when not connected, workspace info + disconnect when connected.
    OAuth popup flow handles connection detection and page refresh.
  </done>
</task>

<task type="auto">
  <name>Task 2: Chat page server component, Connect Slack prompt, and channel header</name>
  <files>
    apps/web/app/(dashboard)/businesses/[id]/chat/page.tsx
    apps/web/_components/slack-connect-prompt.tsx
    apps/web/_components/slack-channel-header.tsx
  </files>
  <action>
    **Update apps/web/app/(dashboard)/businesses/[id]/chat/page.tsx:**
    - Import getSlackStatusAction from slack-actions and getSlackChannelsAction
    - Fetch Slack connection status alongside existing data (Promise.all)
    - Pass slackStatus and slackChannels to ChatLayout
    - Pass business slug for "Open in Slack" deep links
    - Fetch business with id, name, slug instead of just id, name

    **Create apps/web/_components/slack-connect-prompt.tsx:**
    Client component. Secondary CTA shown on the chat page when Slack is not connected. This is NOT the primary connect entry point (that lives on the integrations page per user decision). This prompt directs users there.
    - Large Slack icon/logo area (use MessageSquare or custom SVG)
    - Heading: "Connect Slack to get started"
    - Subtext: "Link your Slack workspace from the Integrations page to route messages between your department agents and Slack channels."
    - Primary button: "Go to Integrations" that navigates to /businesses/{businessId}/integrations (where the real Connect Slack card lives)
    - Secondary text below: "You can connect Slack from the Integrations page"
    - Clean, centered layout matching the B2B SaaS style

    **Create apps/web/_components/slack-channel-header.tsx:**
    Client component. Channel header bar replacing the existing inline header in ChatLayout.
    - Show channel name with # prefix (e.g. "#acme-sales")
    - Department name as subtitle
    - "Connected to Slack" badge (green dot + text) on the right
    - "Open in Slack" button (ExternalLink icon) that opens `https://slack.com/app_redirect?channel={slackChannelId}&team={slackTeamId}` -- deep links directly to the specific channel per user decision
    - Frozen agent indicator (existing Snowflake + badge from current header)
    - Agent status indicators (existing "No active agent" badge)
  </action>
  <verify>
    Run `pnpm turbo typecheck` -- should pass.
    Verify chat page passes slackStatus to ChatLayout.
    Verify SlackConnectPrompt renders without errors.
  </verify>
  <done>
    Chat page fetches Slack status and passes to layout.
    Connect Slack prompt component created with OAuth popup flow.
    Channel header shows Slack connection status and Open in Slack deep link.
  </done>
</task>

<task type="auto">
  <name>Task 3: Refactored chat layout, channel list, message display, and send actions</name>
  <files>
    apps/web/_components/chat-layout.tsx
    apps/web/_components/chat-channel-list.tsx
    apps/web/_components/chat-message-list.tsx
    apps/web/_components/chat-message-bubble.tsx
    apps/web/_components/chat-message-input.tsx
    apps/web/_actions/chat-actions.ts
  </files>
  <action>
    **Refactor apps/web/_components/chat-layout.tsx:**
    - Accept new props: slackStatus (SlackConnectionStatus), slackChannels (SlackChannelMapping[]), businessSlug (string), slackTeamId (string | null)
    - When slackStatus.connected === false: render SlackConnectPrompt (no channel sidebar, no messages)
    - When slackStatus.connected === true: render the Slack-powered chat UI
    - Replace department-based channel navigation with Slack channel navigation (use slackChannels for the sidebar, mapped to department info)
    - Replace the inline channel header with SlackChannelHeader component
    - When sending a message: call sendSlackMessageAction instead of sendMessageAction (posts to Slack AND stores in Supabase)
    - Message polling: continue polling via getMessagesAction every 10 seconds (messages from Slack events are already stored in Supabase by the webhook handler from Plan 01)
    - Remove WebSocket streaming logic (VPS streaming becomes secondary when Slack is the transport)
    - Remove isAgentTyping stub delay logic (agent response comes through Slack, polling picks it up)
    - Keep the existing VPS offline banner but update text: show only when Slack IS connected but VPS agents are offline
    - Archive/remove old conversation history display: when Slack is connected, the message list starts fresh with Slack-synced messages per user decision
    - Existing conversation data remains in DB but is not displayed in the Slack-connected view

    **Update apps/web/_components/chat-channel-list.tsx:**
    - Accept optional slackChannels prop alongside existing channels
    - When slackChannels is provided: show Slack channel names with # prefix (e.g. "#acme-sales") instead of department names
    - Show "Slack Channels" as sidebar header instead of "Channels"
    - Keep existing department icon mapping (match department type from the mapping)
    - Keep unread count badges and frozen indicators
    - Selection identifies by departmentId (same as before, but display shows Slack channel name)

    **Update apps/web/_components/chat-message-bubble.tsx:**
    - Add optional slackUser display: if message.metadata has slackUser info (name, avatar), show Slack user identity
    - For messages from Slack users (role=user with slack_ts metadata): show the Slack username instead of generic "You"
    - For agent messages: continue showing agent name and avatar (agents post to Slack with username override)
    - Minor style updates for a more Slack-like feel: remove bubble-style for agent messages, use flat left-aligned layout with name + avatar on top row, message content below (more like Slack's layout)
    - Keep tool call traces and knowledge source footnotes unchanged

    **Update apps/web/_components/chat-message-input.tsx:**
    - No major changes needed -- the input component is reusable
    - Update placeholder text: "Message #{channelName}" when Slack is connected (instead of generic "Type a message...")
    - Accept optional channelName prop for the placeholder

    **Create/update apps/web/_actions/chat-actions.ts:**
    - Add `sendSlackMessageAction(businessId, departmentId, content, fileMetadata?)`:
      1. Auth check
      2. Get Slack client for business (via getSlackClient)
      3. Look up channel mapping for this department
      4. Post message to Slack channel via client.chat.postMessage (as the admin user's message)
      5. Store message in Supabase messages table with slack_ts and slack_channel_id
      6. The Slack event handler will trigger agent routing when the bot is @mentioned, or we can route immediately here
      7. Per user decision: agents respond when @mentioned. So for admin-initiated messages from the panel: store the message in Supabase, post to Slack, and if the message contains an @mention of the bot, the event handler will trigger routing. Alternatively, auto-@mention the department agent in messages sent from the admin panel so they always get a response.
      8. Return { userMessage } (agent response comes asynchronously via Slack events + polling)
    - Keep existing sendMessageAction for backward compatibility (used when Slack is not connected)
    - Keep existing getMessagesAction, getConversationsAction, getDepartmentChannelsAction unchanged (they still read from Supabase)
  </action>
  <verify>
    Run `pnpm turbo typecheck` -- all packages and apps must pass.
    Verify ChatLayout renders SlackConnectPrompt when not connected.
    Verify ChatLayout renders Slack-powered UI when connected.
    Verify sendSlackMessageAction posts to Slack and stores in Supabase.
  </verify>
  <done>
    Chat layout conditionally renders Connect Slack or Slack-powered UI.
    Channel sidebar shows Slack channel names with # prefix.
    Messages display in flat Slack-like layout.
    Admin can send messages from the panel that appear in Slack.
    Polling picks up agent responses from Slack events.
    Old stub/VPS chat infrastructure replaced when Slack is connected.
  </done>
</task>

</tasks>

<verification>
- `pnpm turbo typecheck` passes across all packages
- Integrations page shows Slack connect card as the primary entry point for OAuth (per user decision)
- Slack connect card shows "Connect Slack" when not connected and workspace info when connected
- Chat page shows secondary CTA directing to integrations page when Slack is not connected
- Chat page shows Slack-powered interface when Slack IS connected
- Channel sidebar shows Slack channel names (e.g. #acme-sales) mapped to departments
- Channel header shows "Connected to Slack" badge and "Open in Slack" button
- "Open in Slack" deep link goes to the specific channel (not just workspace)
- Messages sent from admin panel appear in the mapped Slack channel
- Messages from Slack appear in the admin panel via polling (stored by event handler)
- Old custom chat with stub responses is not shown when Slack is connected
</verification>

<success_criteria>
- SlackConnectCard on integrations page as primary OAuth entry point (per user decision)
- SlackConnectPrompt on chat page as secondary affordance directing to integrations page
- SlackChannelHeader with connection badge and deep link
- Refactored ChatLayout with Slack/non-Slack conditional rendering
- Updated channel list with Slack channel names
- sendSlackMessageAction for bidirectional messaging
- Full typecheck pass
</success_criteria>

<output>
After completion, create `.planning/phases/14-slack-integration-chat-replacement/14-02-SUMMARY.md`
</output>
