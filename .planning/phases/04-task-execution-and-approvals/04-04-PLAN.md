---
phase: 04-task-execution-and-approvals
plan: 04
type: execute
wave: 4
depends_on: ["04-02", "04-03"]
files_modified:
  - apps/web/_components/business-overview.tsx
  - apps/web/_components/usage-summary.tsx
  - apps/web/_components/task-detail-panel.tsx
  - apps/web/app/(dashboard)/businesses/[id]/tasks/[taskId]/page.tsx
  - apps/web/app/(dashboard)/businesses/[id]/page.tsx
  - packages/core/worker/tool-runner.ts
  - packages/core/orchestrator/executor.ts
  - packages/core/approval/approval-service.ts
  - apps/web/_actions/task-actions.ts
autonomous: true
requirements:
  - TOPS-04
  - DASH-09
  - SECR-03
  - SECR-04
  - SECR-05

must_haves:
  truths:
    - "Usage summary on business overview shows total token consumption and cost per tenant"
    - "Task detail page displays token_usage and cost_cents for completed tasks"
    - "Worker metering records usage_records for every task execution"
    - "No service_role client usage exists in worker, orchestrator, or approval code"
    - "All new tables have RLS enabled with is_business_member policies"
    - "Loading, error, and empty states handled on tasks and approvals pages"
  artifacts:
    - path: "apps/web/_components/usage-summary.tsx"
      provides: "Usage stats card showing tokens and cost per agent"
      contains: "usage"
    - path: "apps/web/_components/business-overview.tsx"
      provides: "Business overview with usage summary and live task/approval counts"
      contains: "UsageSummary"
  key_links:
    - from: "apps/web/_components/usage-summary.tsx"
      to: "packages/core/worker/metering.ts"
      via: "displays data from getUsageSummary"
      pattern: "getUsageSummary"
    - from: "apps/web/app/(dashboard)/businesses/[id]/page.tsx"
      to: "packages/core/server.ts"
      via: "fetches usage summary and task counts for overview"
      pattern: "getUsageSummary"
---

<objective>
Wire usage metering into the dashboard, add token/cost display to task details, and harden security across all Phase 4 code.

Purpose: This plan completes the Phase 4 deliverables by making usage data visible to admins, ensuring every task execution records metering data, and performing a security audit to verify sandbox enforcement, RLS coverage, and no service_role leaks. It also polishes loading/error/empty states across the new pages.

Output: Usage summary component on business overview, token/cost display on task detail, metering wired into executor, security verification, and UI polish.
</objective>

<execution_context>
@/Users/tjbrooks/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tjbrooks/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-task-execution-and-approvals/04-CONTEXT.md
@.planning/phases/04-task-execution-and-approvals/04-RESEARCH.md
@.planning/phases/04-task-execution-and-approvals/04-01-SUMMARY.md
@.planning/phases/04-task-execution-and-approvals/04-02-SUMMARY.md
@.planning/phases/04-task-execution-and-approvals/04-03-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Usage summary UI, metering wiring, and task detail token display</name>
  <files>
    apps/web/_components/usage-summary.tsx
    apps/web/_components/business-overview.tsx
    apps/web/app/(dashboard)/businesses/[id]/page.tsx
    apps/web/_components/task-detail-panel.tsx
    apps/web/app/(dashboard)/businesses/[id]/tasks/[taskId]/page.tsx
    packages/core/orchestrator/executor.ts
  </files>
  <action>
    **apps/web/_components/usage-summary.tsx:**
    "use client" component displaying usage stats:
    - Accept usageSummary object as props: { totalPromptTokens, totalCompletionTokens, totalCostCents, byAgent: [{agentId, agentName, promptTokens, completionTokens, costCents}] }
    - Card layout with:
      - Total tokens (prompt + completion) formatted with commas
      - Total cost formatted as dollars (costCents / 100, 2 decimal places)
      - Per-agent breakdown as a compact table: Agent Name, Tokens, Cost
      - If no usage data: "No usage recorded yet" muted text
    - Small, dashboard-card sized -- fits in the business overview grid

    **apps/web/app/(dashboard)/businesses/[id]/page.tsx:**
    Update the business overview Server Component:
    - Add query to fetch usage summary: aggregate from usage_records where business_id matches. Use SQL: `SELECT agent_id, SUM(prompt_tokens) as prompt_tokens, SUM(completion_tokens) as completion_tokens, SUM(cost_cents) as cost_cents FROM usage_records WHERE business_id = $1 GROUP BY agent_id`
    - Or use getUsageSummary from core/server if it can be called with the supabase client
    - Fetch pending approval count: `supabase.from('approvals').select('id', { count: 'exact', head: true }).eq('business_id', businessId).eq('status', 'pending')`
    - Fetch active task count: `supabase.from('tasks').select('id', { count: 'exact', head: true }).eq('business_id', businessId).in('status', ['queued', 'assigned', 'in_progress', 'waiting_approval'])`
    - Pass usageSummary, pendingApprovalCount, and activeTaskCount to BusinessOverview

    **apps/web/_components/business-overview.tsx:**
    - Import and render UsageSummary component in the overview grid
    - Replace hardcoded pendingApprovalCount with the real value from props
    - Add active task count display to the overview stats
    - Tasks quick link shows active count badge
    - Approvals quick link shows pending count badge

    **apps/web/_components/task-detail-panel.tsx:**
    Add token usage and cost display:
    - If task has token_usage with non-zero values, show a "Usage" section:
      - Prompt tokens: X, Completion tokens: Y
      - Estimated cost: $Z.ZZ
    - If task is a parent, show aggregate usage across subtasks

    **apps/web/app/(dashboard)/businesses/[id]/tasks/[taskId]/page.tsx:**
    - Fetch usage_records for this task: `supabase.from('usage_records').select('*').eq('task_id', taskId)`
    - Pass usage records to the task detail components
    - Show token breakdown in the detail view

    **packages/core/orchestrator/executor.ts:**
    Ensure metering is wired into the execution flow:
    - After runAgentTask completes, call recordUsage with the simulated token counts
    - Update the task record with token_usage and cost_cents from metering
    - This should already be partially wired from 04-02; verify and complete the integration

    Run `pnpm turbo typecheck` to verify.
  </action>
  <verify>
    Run `pnpm turbo typecheck` (no type errors).
    Verify usage-summary.tsx renders total tokens and cost with per-agent breakdown.
    Verify business overview page.tsx fetches usage summary, pending approvals count, and active tasks count.
    Verify business-overview.tsx displays real counts instead of hardcoded values.
    Verify task detail panel shows token_usage and cost when available.
    Verify executor.ts calls recordUsage after task execution.
  </verify>
  <done>
    Usage summary displays on business overview with total tokens and cost per agent. Task detail pages show token usage and cost for completed tasks. Metering fully wired into orchestrator executor. Business overview shows live approval and task counts.
  </done>
</task>

<task type="auto">
  <name>Task 2: Security audit, error handling, and UI polish</name>
  <files>
    packages/core/worker/tool-runner.ts
    packages/core/worker/sandbox.ts
    packages/core/orchestrator/executor.ts
    packages/core/approval/approval-service.ts
    apps/web/_actions/task-actions.ts
    apps/web/_actions/approval-actions.ts
    apps/web/_components/tasks-table.tsx
    apps/web/_components/tasks-kanban.tsx
    apps/web/_components/approvals-list.tsx
  </files>
  <action>
    **Security audit -- verify across all Phase 4 files:**

    1. **No service_role usage (SECR-05):** Grep all files in packages/core/worker/, packages/core/orchestrator/, packages/core/approval/, and apps/web/_actions/ for "service_role", "createClient", or "SUPABASE_SERVICE_ROLE". The ONLY exception is the webhook ingestion route (04-01) which is documented. If any other file uses service_role, refactor to use the passed-in RLS-scoped client.

    2. **RLS on all tables (SECR-03):** Verify every new table (tasks, subtask_dependencies, assistance_requests, approvals, approval_policies, usage_records) has `ENABLE ROW LEVEL SECURITY` and at least a SELECT policy using is_business_member.

    3. **Sandbox enforcement (SECR-03, SECR-04):** Verify tool-runner.ts calls assertSandbox before every tool execution. Verify sandbox.ts checks all BLOCKED_CAPABILITIES. Verify validateToolAccess checks both department catalog and agent tool_profile.

    4. **Audit log coverage (APRV-07):** Verify audit_log entries are created for: task.created, task.status_changed, task.routed, approval.created, approval.approved, approval.rejected, approval.guidance_provided, assistance.created, assistance.responded.

    **Error handling hardening:**

    5. **packages/core/worker/tool-runner.ts:** Wrap tool execution in try/catch. On error, return { success: false, error: message } instead of throwing. Log error with console.error.

    6. **packages/core/orchestrator/executor.ts:** If routing fails (no active agents), create assistance_request instead of throwing. If worker fails, create assistance_request per CONTEXT decision (agent asks for help, does NOT auto-fail).

    7. **packages/core/approval/approval-service.ts:** Validate approval exists before operating. Return descriptive errors for: approval not found, invalid transition, already decided.

    8. **apps/web/_actions/task-actions.ts and approval-actions.ts:** Ensure all Server Actions have consistent error handling: try/catch around service calls, return { error: message } on failure, never let exceptions propagate to client.

    **UI polish:**

    9. **apps/web/_components/tasks-table.tsx:** Add loading skeleton (Suspense boundary or loading prop). Add empty state message. Ensure table is responsive.

    10. **apps/web/_components/tasks-kanban.tsx:** Add empty state for columns with no tasks (show muted "No tasks" text). Ensure columns don't collapse when empty.

    11. **apps/web/_components/approvals-list.tsx:** Add loading state. Add empty state ("No pending approvals"). Ensure bulk action bar only shows when items are selected. Add confirmation dialog for bulk reject.

    Run `pnpm turbo typecheck` and `pnpm turbo build` to verify full compilation.
  </action>
  <verify>
    Run `pnpm turbo typecheck` (no type errors).
    Run `pnpm turbo build` (no build errors).
    Grep for "service_role" in packages/core/worker/, packages/core/orchestrator/, packages/core/approval/ -- must find zero occurrences.
    Verify tool-runner.ts has try/catch around execution with error return.
    Verify executor.ts creates assistance_request on routing failure.
    Verify all Server Actions in task-actions.ts and approval-actions.ts have try/catch with { error } return.
    Verify tasks-table.tsx has empty state.
    Verify tasks-kanban.tsx has empty column state.
    Verify approvals-list.tsx has empty state and loading state.
  </verify>
  <done>
    Security audit confirms: no service_role usage outside webhook endpoint, RLS on all tables, sandbox enforcement before every tool execution, and full audit log coverage. Error handling hardened with try/catch and assistance requests on failure. UI components have loading, error, and empty states.
  </done>
</task>

</tasks>

<verification>
- `pnpm turbo typecheck` passes
- `pnpm turbo build` passes
- Usage summary visible on business overview with token and cost totals
- Task detail shows token_usage and cost for completed tasks
- No service_role in worker/orchestrator/approval code (grep confirms)
- All 6 new tables have RLS enabled with appropriate policies
- Sandbox blocks all BLOCKED_CAPABILITIES
- All 9 audit log action types present in code
- Tasks page has loading and empty states
- Approvals page has loading and empty states
- Kanban columns show "No tasks" when empty
</verification>

<success_criteria>
Internal usage metering tracks token consumption and cost per tenant per agent with visible dashboard summary (TOPS-04). Tasks and approvals pages fully functional with polish (DASH-09 completion). Agent execution sandboxed with no host filesystem access validated by policy checks (SECR-03). Tool access validated against tenant-scoped allowlists (SECR-04). Agents never run with service_role credentials verified by security audit (SECR-05).
</success_criteria>

<output>
After completion, create `.planning/phases/04-task-execution-and-approvals/04-04-SUMMARY.md`
</output>
