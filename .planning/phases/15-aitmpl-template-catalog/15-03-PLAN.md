---
phase: 15-aitmpl-template-catalog
plan: 03
type: execute
wave: 3
depends_on:
  - 15-02
files_modified:
  - apps/web/_components/aitmpl-suggestion-banner.tsx
  - apps/web/app/businesses/[businessId]/page.tsx
  - apps/web/_components/skill-template-browser.tsx
  - apps/web/_components/health-dashboard.tsx
autonomous: true
requirements:
  - AITMPL-01
  - AITMPL-02
  - AITMPL-03
  - AITMPL-04

must_haves:
  truths:
    - "AitmplSuggestionBanner is a dismissible banner that appears on the business dashboard page"
    - "Banner text says 'Enhance your agents with AITMPL templates' with a 'Browse Catalog' button"
    - "Banner dismisses on explicit dismiss click or after one catalog browse session"
    - "Dismissed state persists via localStorage keyed by businessId"
    - "Browse Catalog button opens AitmplCatalogBrowser dialog"
    - "Skill Template Browser has a new 'Browse AITMPL Skills' button alongside existing template cards"
    - "Recommendation badges show on AITMPL catalog cards based on department context"
    - "Business dashboard page renders AitmplSuggestionBanner below the header"
  artifacts:
    - path: "apps/web/_components/aitmpl-suggestion-banner.tsx"
      provides: "Dismissible banner suggesting AITMPL catalog browsing after business creation"
      exports: ["AitmplSuggestionBanner"]
    - path: "apps/web/app/businesses/[businessId]/page.tsx"
      provides: "Business dashboard with AitmplSuggestionBanner integrated"
      contains: "AitmplSuggestionBanner"
    - path: "apps/web/_components/skill-template-browser.tsx"
      provides: "Updated Skill Template Browser with Browse AITMPL Skills link"
      contains: "AitmplCatalogBrowser"
    - path: "apps/web/_components/health-dashboard.tsx"
      provides: "Updated Health Dashboard with AITMPL banner slot"
      contains: "AitmplSuggestionBanner"
  key_links:
    - from: "apps/web/_components/aitmpl-suggestion-banner.tsx"
      to: "apps/web/_components/aitmpl-catalog-browser.tsx"
      via: "Browse Catalog button opens AitmplCatalogBrowser"
      pattern: "AitmplCatalogBrowser"
    - from: "apps/web/_components/skill-template-browser.tsx"
      to: "apps/web/_components/aitmpl-catalog-browser.tsx"
      via: "Browse AITMPL Skills button opens AitmplCatalogBrowser"
      pattern: "AitmplCatalogBrowser"
---

<objective>
AITMPL suggestion banner on business dashboard, Skill Template Browser integration, and final polish.

Purpose: This plan completes the AITMPL catalog integration by adding the post-creation suggestion banner on the business dashboard, integrating AITMPL as a source in the existing Skill Template Browser, and ensuring all access points work with recommendation context. The banner is the primary discovery mechanism for new businesses, while the Skill Template Browser integration makes AITMPL a peer source alongside local templates.

Output: 1 new component (suggestion banner), 3 modified components (dashboard page, skill template browser, health dashboard).
</objective>

<execution_context>
@/Users/tjbrooks/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tjbrooks/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-aitmpl-template-catalog/15-CONTEXT.md
@.planning/phases/15-aitmpl-template-catalog/15-RESEARCH.md
@.planning/phases/15-aitmpl-template-catalog/15-01-PLAN.md
@.planning/phases/15-aitmpl-template-catalog/15-02-PLAN.md
@apps/web/_components/aitmpl-catalog-browser.tsx
@apps/web/_components/skill-template-browser.tsx
@apps/web/_components/health-dashboard.tsx
@apps/web/app/businesses/[businessId]/page.tsx
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: AITMPL suggestion banner component</name>
  <files>
    apps/web/_components/aitmpl-suggestion-banner.tsx
  </files>
  <action>
    **apps/web/_components/aitmpl-suggestion-banner.tsx** (NEW):
    "use client" dismissible banner for the business dashboard.

    Props:
    ```typescript
    interface AitmplSuggestionBannerProps {
      businessId: string;
      /** Available agents for the catalog browser target picker */
      agents: Array<{ id: string; name: string; department_name?: string }>;
      /** Available departments for the catalog browser target picker */
      departments: Array<{ id: string; name: string }>;
    }
    ```

    State:
    - `dismissed: boolean` — initialized from `localStorage.getItem(\`aitmpl-banner-dismissed-${businessId}\`)` on mount (use useEffect to avoid SSR mismatch)
    - `catalogOpen: boolean` — controls AitmplCatalogBrowser dialog

    localStorage key: `aitmpl-banner-dismissed-${businessId}`

    Dismiss logic:
    - On explicit dismiss (X button): set `dismissed = true`, write to localStorage
    - On catalog browser close after at least one import: set `dismissed = true`, write to localStorage
    - Pass `onImported` to AitmplCatalogBrowser that sets a "hasImported" ref, then on `onOpenChange(false)` check the ref

    Render (when not dismissed):
    - A styled card/banner: `rounded-lg border border-blue-200 bg-blue-50 dark:border-blue-800 dark:bg-blue-900/30 p-4`
    - Row layout: icon (Sparkles from lucide-react) + text + buttons
    - Text: **"Enhance your agents with AITMPL templates"** as bold heading, then sub-text: "Browse 1,600+ pre-built skills, agents, commands, and tools from the AITMPL community catalog."
    - "Browse Catalog" Button (variant="default", size="sm") — opens catalog browser
    - "Dismiss" Button (variant="ghost", size="sm", X icon) — dismisses banner
    - The AitmplCatalogBrowser rendered inline (hidden when `catalogOpen = false`)

    When `dismissed = true` on mount (from localStorage), return `null` (render nothing).

    Keep component under 80 lines.
  </action>
  <verify>
    Run `pnpm turbo typecheck` -- apps/web must pass.
    Verify banner renders with correct text and buttons.
    Verify dismiss persists to localStorage.
    Verify Browse Catalog opens AitmplCatalogBrowser.
  </verify>
  <done>
    Dismissible AITMPL suggestion banner with Browse Catalog button.
    Dismiss state persists via localStorage per business.
    Banner auto-dismisses after a catalog browse with import.
  </done>
</task>

<task type="auto">
  <name>Task 2: Dashboard integration, Skill Template Browser AITMPL source, and final wiring</name>
  <files>
    apps/web/app/businesses/[businessId]/page.tsx
    apps/web/_components/health-dashboard.tsx
    apps/web/_components/skill-template-browser.tsx
  </files>
  <action>
    **apps/web/app/businesses/[businessId]/page.tsx** OR **apps/web/_components/health-dashboard.tsx** (MODIFY):
    Integrate AitmplSuggestionBanner on the business dashboard.

    First, read the actual page.tsx and health-dashboard.tsx to determine where the banner should go. The business dashboard page renders HealthDashboard (per Phase 5 decisions). The banner should appear between the page header and the health dashboard content.

    Option A (if page.tsx is a Server Component):
    - Import `AitmplSuggestionBanner` in the page
    - Fetch agents and departments from supabase for the banner props
    - Render banner between header and HealthDashboard
    - Pass agents/departments to banner

    Option B (if health-dashboard.tsx is more appropriate):
    - Accept `agents` and `departments` as optional props
    - Render AitmplSuggestionBanner at the top of the dashboard
    - Pass businessId from existing props

    Choose the approach that minimizes prop-drilling. If HealthDashboard already has access to agents data, prefer Option B. **Executor decision point:** Read actual file contents before choosing. Both options are valid.

    The banner needs agents and departments lists for the catalog browser's target picker. These can be:
    - Fetched server-side in page.tsx and passed down
    - Or fetched client-side via Server Actions inside the banner (simpler but slower)

    Prefer server-side fetch in page.tsx for faster initial render:
    ```typescript
    // In page.tsx server component
    const { data: agents } = await supabase
      .from("agents")
      .select("id, name, departments(name)")
      .eq("business_id", businessId)
      .eq("status", "active");

    const { data: departments } = await supabase
      .from("departments")
      .select("id, name")
      .eq("business_id", businessId);
    ```

    Map agents to the expected shape: `agents.map(a => ({ id: a.id, name: a.name, department_name: (a.departments as any)?.name }))`

    **apps/web/_components/skill-template-browser.tsx** (MODIFY):
    Add AITMPL as a source alongside existing local templates.

    Changes:
    - Import `AitmplCatalogBrowser` from `./aitmpl-catalog-browser`
    - Import `useState` (already imported)
    - Add state: `const [aitmplOpen, setAitmplOpen] = useState(false);`
    - Accept new optional props: `agents?: Array<{ id: string; name: string; department_name?: string }>`, `departments?: Array<{ id: string; name: string }>`, `departmentType?: string`
    - In the dialog header area (after DialogDescription), add a button row:
      - "Browse AITMPL Skills" Button (variant="outline", size="sm") with external link icon — only render this button if `agents` prop is provided (non-empty array)
      - Click opens the AITMPL catalog browser with `defaultType="skill"` and `departmentType` prop
    - Render `<AitmplCatalogBrowser>` at the bottom of the dialog content:
      ```tsx
      <AitmplCatalogBrowser
        open={aitmplOpen}
        onOpenChange={setAitmplOpen}
        businessId={businessId}
        defaultType="skill"
        departmentType={departmentType}
        agentId={agentId}
        agents={agents ?? []}
        departments={departments ?? []}
        onImported={() => {
          onSkillAdded?.();
          setAitmplOpen(false);
        }}
      />
      ```
    - This creates a layered dialog: skill template browser shows local templates, "Browse AITMPL Skills" opens the AITMPL catalog as a second dialog on top

    Keep the existing template card grid, filters, and preview section unchanged. The AITMPL button is an additive enhancement.
  </action>
  <verify>
    Run `pnpm turbo typecheck` -- all packages and apps must pass.
    Verify AitmplSuggestionBanner appears on the business dashboard page.
    Verify the banner has "Browse Catalog" and dismiss buttons.
    Verify dismiss persists via localStorage.
    Verify Skill Template Browser has "Browse AITMPL Skills" button.
    Verify clicking the AITMPL button opens the catalog browser from within the skill template browser.
    Verify agents and departments are available for target picker in all access points.
  </verify>
  <done>
    AitmplSuggestionBanner rendered on business dashboard page.
    Skill Template Browser enhanced with "Browse AITMPL Skills" button as peer source.
    Dashboard fetches agents/departments server-side for catalog browser target picker.
    All AITMPL access points connected: dashboard banner, skill template browser, agent skills tab, agent config.
  </done>
</task>

</tasks>

<verification>
- `pnpm turbo typecheck` passes across all packages
- AitmplSuggestionBanner renders on the business dashboard with correct styling
- Banner dismiss persists via localStorage keyed by businessId
- Browse Catalog button opens AitmplCatalogBrowser dialog from the banner
- Skill Template Browser shows "Browse AITMPL Skills" button
- AITMPL catalog browser opens from within the Skill Template Browser
- All 4 access points work: dashboard banner, skill template browser, agent skills tab, agent config tool profile
- Recommendation badges appear on cards matching department context across all access points
- MCP imports from any access point show merge preview before confirming
</verification>

<success_criteria>
Complete AITMPL catalog integration across the application. Users discover the catalog via a dashboard banner after business creation. Skill Template Browser offers AITMPL as a peer source alongside local templates. Agent Skills tab and Config tab provide context-aware browsing. All 7 AITMPL categories (Skills, Agents, Commands, MCPs, Settings, Hooks, Plugins) are browsable with recommendations based on department type. Full typecheck pass.
</success_criteria>

<output>
After completion, create `.planning/phases/15-aitmpl-template-catalog/15-03-SUMMARY.md`
</output>
