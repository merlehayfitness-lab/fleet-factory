---
phase: 16-tenant-disable-fix-dashboard-freeze
plan: 03
type: execute
wave: 3
depends_on:
  - 16-01
  - 16-02
files_modified:
  - apps/web/app/(dashboard)/businesses/[id]/layout.tsx
  - apps/web/_components/health-dashboard.tsx
  - apps/web/_components/deploy-button.tsx
  - apps/web/_components/deployment-center.tsx
  - apps/web/_components/chat-layout.tsx
  - apps/web/_components/chat-message-input.tsx
  - apps/web/_components/agent-tree-view.tsx
  - apps/web/app/(dashboard)/businesses/[id]/tasks/new/page.tsx
  - apps/web/app/(dashboard)/businesses/[id]/page.tsx
autonomous: true
requirements:
  - TFIX-01
  - TFIX-02
  - TFIX-03
  - TFIX-04

must_haves:
  truths:
    - "Business [id] layout fetches business status, wraps children in BusinessStatusProvider, and renders SuspendedBanner when disabled"
    - "Layout no longer returns notFound() for disabled businesses -- it renders the frozen dashboard instead"
    - "SuspendedBanner appears at the top of every sub-page when business is disabled/suspended"
    - "All sidebar navigation links remain fully functional when business is disabled"
    - "Deploy/Redeploy/Rollback buttons are disabled with tooltip when business is disabled"
    - "Chat message input is disabled with placeholder 'Business is suspended' when disabled"
    - "Create Task button/link is disabled when business is disabled"
    - "Health dashboard shows the disabled status correctly via StatusBadge"
    - "Health dashboard displays a VPS warning indicator when VPS container stop failed during disable"
    - "Read-only content (tables, logs, history, conversations) remains fully interactive"
    - "Restore button in SuspendedBanner triggers restore with confirm dialog and refreshes the page"
    - "useBusinessStatus hook used by client components to check isDisabled for conditional UI"
  artifacts:
    - path: "apps/web/app/(dashboard)/businesses/[id]/layout.tsx"
      provides: "Business layout with status check, context provider, and banner rendering"
      contains: "BusinessStatusProvider"
    - path: "apps/web/_components/deploy-button.tsx"
      provides: "Deploy button that respects disabled state from useBusinessStatus"
      contains: "useBusinessStatus"
    - path: "apps/web/_components/chat-layout.tsx"
      provides: "Chat layout with disabled input when business is suspended"
      contains: "useBusinessStatus"
    - path: "apps/web/_components/health-dashboard.tsx"
      provides: "Health dashboard with VPS warning indicator for failed container stops"
      contains: "vps_warning"
  key_links:
    - from: "apps/web/app/(dashboard)/businesses/[id]/layout.tsx"
      to: "apps/web/_components/business-status-provider.tsx"
      via: "Layout wraps children in BusinessStatusProvider with status from DB"
      pattern: "BusinessStatusProvider"
    - from: "apps/web/app/(dashboard)/businesses/[id]/layout.tsx"
      to: "apps/web/_components/suspended-banner.tsx"
      via: "Layout conditionally renders SuspendedBanner when isDisabled"
      pattern: "SuspendedBanner"
    - from: "apps/web/_components/deploy-button.tsx"
      to: "apps/web/_components/business-status-provider.tsx"
      via: "Deploy button uses useBusinessStatus to disable when suspended"
      pattern: "useBusinessStatus"
    - from: "apps/web/_components/chat-layout.tsx"
      to: "apps/web/_components/business-status-provider.tsx"
      via: "Chat layout uses useBusinessStatus to disable input"
      pattern: "useBusinessStatus"
    - from: "apps/web/_components/health-dashboard.tsx"
      to: "packages/core/emergency/emergency-service.ts"
      via: "Health dashboard reads latest emergency audit log entry to detect vps_warning metadata"
      pattern: "vps_warning"
---

<objective>
Integrate BusinessStatusProvider and SuspendedBanner into the business layout, update key client components to respect the disabled state, and surface VPS warning indicators on the health dashboard.

Purpose: This plan completes the UI enforcement layer. It modifies the business [id] layout to fetch status, render the suspended banner, and provide context to all children. It then updates the major mutation-bearing client components (deploy button, deployment center, chat input, task creation, agent tree) to use the useBusinessStatus() hook and disable their mutation controls when the business is disabled. The health dashboard gets a VPS warning indicator when container stop failed during disable. Read-only content remains fully interactive.

Output: Modified layout with context/banner, updated deploy button, deployment center, chat layout, chat input, agent tree view, task creation page, health dashboard with VPS warning -- all respecting disabled state.
</objective>

<execution_context>
@/Users/tjbrooks/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tjbrooks/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-tenant-disable-fix-dashboard-freeze/16-CONTEXT.md
@.planning/phases/16-tenant-disable-fix-dashboard-freeze/16-RESEARCH.md
@.planning/phases/16-tenant-disable-fix-dashboard-freeze/16-01-PLAN.md
@apps/web/app/(dashboard)/businesses/[id]/layout.tsx
@apps/web/_components/health-dashboard.tsx
@apps/web/_components/deploy-button.tsx
@apps/web/_components/chat-layout.tsx
@apps/web/_components/business-status-provider.tsx
@apps/web/_components/suspended-banner.tsx
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate BusinessStatusProvider and SuspendedBanner into business layout</name>
  <files>
    apps/web/app/(dashboard)/businesses/[id]/layout.tsx
    apps/web/app/(dashboard)/businesses/[id]/page.tsx
  </files>
  <action>
    **apps/web/app/(dashboard)/businesses/[id]/layout.tsx** (MODIFY):
    This is the critical change -- the layout is the single enforcement point for the frozen dashboard.

    Replace the entire file content:

    1. Keep existing imports: `notFound`, `createServerClient`
    2. Add new imports:
       ```typescript
       import { BusinessStatusProvider } from "@/_components/business-status-provider";
       import { SuspendedBanner } from "@/_components/suspended-banner";
       ```
    3. Update the business query to select `id, name, status`:
       ```typescript
       const { data: business, error } = await supabase
         .from("businesses")
         .select("id, name, status")
         .eq("id", id)
         .single();
       ```
    4. Keep the `if (error || !business) { notFound(); }` check -- this only triggers if the business truly doesn't exist or RLS blocks access. A disabled business still returns data, so it will NOT 404 anymore.
    5. Determine disabled state:
       ```typescript
       const isDisabled = business.status === "disabled" || business.status === "suspended";
       ```
    6. Wrap children in BusinessStatusProvider and conditionally render SuspendedBanner:
       ```tsx
       return (
         <BusinessStatusProvider
           status={business.status as string}
           businessId={business.id as string}
           businessName={business.name as string}
         >
           {isDisabled && (
             <SuspendedBanner
               businessId={business.id as string}
               businessName={business.name as string}
               status={business.status as string}
             />
           )}
           {children}
         </BusinessStatusProvider>
       );
       ```

    The banner renders ABOVE the children (pushing content down) because it's a sticky element. All sidebar navigation remains unaffected (sidebar is in the parent dashboard layout).

    **apps/web/app/(dashboard)/businesses/[id]/page.tsx** (MODIFY):
    The overview page currently calls `notFound()` if business is missing. Keep this behavior but no other changes needed -- the HealthDashboard component already receives `business.status` as part of the business prop. It renders a StatusBadge that will correctly show "Disabled" or "Suspended".

    No changes needed to this file if the StatusBadge already handles disabled/suspended status strings. Verify the health dashboard page does not have its own `notFound()` call that would trigger before the layout catches it.

    Actually, check: the page does `if (businessError || !business) { notFound(); }`. Since the business still exists (just disabled), this won't trigger. No change needed.
  </action>
  <verify>
    Run `pnpm turbo typecheck` -- must pass.
    Verify layout wraps children in BusinessStatusProvider.
    Verify layout renders SuspendedBanner when isDisabled is true.
    Verify layout still calls notFound() only when business genuinely doesn't exist.
    Verify the banner renders above children (check JSX order).
  </verify>
  <done>
    Business layout fetches status, provides context, and renders suspended banner. Disabled businesses show a frozen dashboard instead of 404. All sub-pages inherit the context and banner.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update client components to respect disabled state and show VPS warning</name>
  <files>
    apps/web/_components/deploy-button.tsx
    apps/web/_components/deployment-center.tsx
    apps/web/_components/chat-layout.tsx
    apps/web/_components/chat-message-input.tsx
    apps/web/_components/health-dashboard.tsx
    apps/web/_components/agent-tree-view.tsx
    apps/web/app/(dashboard)/businesses/[id]/tasks/new/page.tsx
  </files>
  <action>
    Import `useBusinessStatus` from `@/_components/business-status-provider` in each client component that has mutation triggers.

    **apps/web/_components/deploy-button.tsx** (MODIFY):
    This is the primary deploy trigger button.
    1. Import `useBusinessStatus`
    2. At the top of the component, call: `const { isDisabled } = useBusinessStatus();`
    3. Add `disabled={isDisabled || existingDisabledCondition}` to the deploy button
    4. Add `title={isDisabled ? "Business is suspended" : undefined}` for native tooltip
    5. Add `className` with conditional opacity: `isDisabled ? "opacity-50 cursor-not-allowed" : ""`

    **apps/web/_components/deployment-center.tsx** (MODIFY):
    The deployment center contains retry and rollback buttons.
    1. Import `useBusinessStatus`
    2. At the top of the component: `const { isDisabled } = useBusinessStatus();`
    3. Find all "Retry", "Rollback", and "Redeploy" buttons and add:
       - `disabled={isDisabled || existingCondition}`
       - `title={isDisabled ? "Business is suspended" : undefined}`
    4. If there's a RollbackDialog trigger button, disable it similarly
    5. Read-only elements (deployment list, artifact viewer, status badges) remain unchanged

    **apps/web/_components/chat-layout.tsx** (MODIFY):
    The chat layout manages the message input area.
    1. Import `useBusinessStatus`
    2. At the top of the component: `const { isDisabled } = useBusinessStatus();`
    3. Pass `isDisabled` down to the ChatMessageInput component (or the inline input area)
    4. When isDisabled is true:
       - The message input should be disabled with placeholder: "Business is suspended"
       - Send button should be disabled
       - The conversation history (ChatMessageList) remains fully scrollable and readable
       - File upload button should be disabled
    5. Keep all existing frozen-agent detection logic -- isDisabled is an additional overlay on top of that

    **apps/web/_components/chat-message-input.tsx** (MODIFY -- if it exists as a separate component):
    If the chat input is a separate component:
    1. Add `isDisabled?: boolean` prop
    2. When isDisabled: `disabled` on the textarea/input, placeholder "Business is suspended", disabled on send button
    3. If the input is inline in chat-layout.tsx, apply the changes there instead

    **apps/web/_components/health-dashboard.tsx** (MODIFY):
    The health dashboard is the main overview component.
    1. Import `useBusinessStatus`
    2. At the top of the component: `const { isDisabled } = useBusinessStatus();`
    3. Find any action buttons on the dashboard (emergency controls dropdown, deploy quick-link, etc.) and disable them:
       - Disable the "Deploy" quick-link/button if it exists
       - The emergency controls in the Settings dropdown should remain functional (they are exempt)
       - The health auto-refresh polling should continue even when disabled (read-only data is fine)
    4. If the dashboard has any inline action cards, add disabled treatment
    5. Keep all existing read-only elements (charts, badges, activity feed) unchanged
    6. **VPS Warning Indicator** (per locked CONTEXT.md decision -- "Show a persistent warning badge on the dashboard: 'VPS containers may still be running.'"):
       - When `isDisabled` is true, query the latest audit log entry for the business where `action = 'tenant_disabled'`
       - Check if the audit log metadata contains `vps_warning` (a truthy string value like "VPS containers may still be running")
       - If `vps_warning` is present in the metadata, render a warning badge/indicator near the status section:
         - Use an `AlertTriangle` icon from lucide-react with amber/yellow styling
         - Text: the `vps_warning` string from metadata (e.g., "VPS containers may still be running")
         - Style: `border-amber-300 bg-amber-50 text-amber-800 dark:border-amber-800 dark:bg-amber-900/30 dark:text-amber-300` with `px-3 py-2 rounded-md text-sm flex items-center gap-2`
       - Two approaches for fetching the audit log:
         - **Option A (preferred):** Accept a `vpsWarning?: string` prop from the parent page component, which fetches the latest `tenant_disabled` audit log in the server component and extracts `metadata.vps_warning`
         - **Option B:** Use a client-side fetch to get the latest audit log entry (heavier, less preferred)
       - Use Option A: In the business overview **page.tsx** server component, after fetching the business, if business is disabled, query:
         ```typescript
         const { data: lastDisableLog } = await supabase
           .from("audit_logs")
           .select("metadata")
           .eq("business_id", business.id)
           .eq("action", "tenant_disabled")
           .order("created_at", { ascending: false })
           .limit(1)
           .single();
         const vpsWarning = lastDisableLog?.metadata?.vps_warning ?? null;
         ```
         Pass `vpsWarning` as a prop to HealthDashboard.
       - In health-dashboard.tsx, accept `vpsWarning?: string | null` prop and render the warning indicator when truthy

    **apps/web/_components/agent-tree-view.tsx** (MODIFY):
    The agent tree has '+' buttons for creating new agents.
    1. Import `useBusinessStatus`
    2. At the top of the component: `const { isDisabled } = useBusinessStatus();`
    3. Pass `isDisabled` to AgentTreeDepartment and AgentTreeNode child components
    4. When isDisabled:
       - '+' buttons on departments and lead agents should be disabled (opacity-50, cursor-not-allowed, no navigation on click)
       - Agent pills remain clickable for viewing (read-only navigation is allowed)
       - Collapse/expand remains functional

    **apps/web/app/(dashboard)/businesses/[id]/tasks/new/page.tsx** (MODIFY):
    The new task page has a task creation form.
    This is a Server Component, so it cannot use useBusinessStatus directly.
    Two approaches:
    - Option A: Re-fetch business status in the server component and pass isDisabled to the form
    - Option B: Make the task form a client component that uses useBusinessStatus

    Use Option A (simpler, keeps existing pattern):
    1. Add a business status query at the top of the page component
    2. If business is disabled, render a message: "Cannot create tasks while business is suspended." with a link back to the tasks page
    3. OR render the form but pass `disabled` prop and let the form component disable its submit button

    Actually, per the context decisions: "All sidebar links remain fully navigable when disabled. Every sub-page loads normally in read-only mode with the suspended banner." So the page should load but the form should be disabled.

    Simplest approach: Since the task form is likely a client component, import useBusinessStatus there. If it's a server component form, add `disabled` attribute to the submit button based on a re-fetched status check (React request deduplication will reuse the layout's query).

    Check the actual component structure and apply whichever pattern fits.

    For ALL components: Use native `title` attribute for tooltip on disabled elements rather than installing a tooltip library. This matches the context decision for "Disabled Button Treatment": "Hover shows a tooltip: 'Business is suspended'".

    **Disabled style pattern** (apply consistently):
    ```
    className={cn(existingClasses, isDisabled && "opacity-50 cursor-not-allowed")}
    disabled={isDisabled || otherCondition}
    title={isDisabled ? "Business is suspended" : undefined}
    ```

    The `cn()` utility is already available throughout the codebase for class merging.
  </action>
  <verify>
    Run `pnpm turbo typecheck` -- all packages and apps must pass.
    Verify deploy button is disabled when business is disabled.
    Verify retry/rollback buttons in deployment center are disabled.
    Verify chat message input is disabled with "Business is suspended" placeholder.
    Verify chat conversation history remains scrollable.
    Verify agent tree '+' buttons are disabled but view navigation still works.
    Verify health dashboard shows VPS warning indicator when vps_warning metadata is present.
    Verify health dashboard read-only data continues to poll and render.
    Verify task creation is blocked when disabled.
    Verify native title tooltip shows "Business is suspended" on hover of disabled buttons.
    Verify no new library dependencies added (native title for tooltips).
  </verify>
  <done>
    All major client components respect the disabled state: deploy button, deployment center, chat input, agent tree creation, and task creation are disabled with tooltip. Health dashboard displays VPS warning indicator when VPS container stop failed. Read-only content (logs, history, tables, conversations) remains fully interactive. Emergency restore remains functional via the SuspendedBanner and Settings dropdown.
  </done>
</task>

</tasks>

<verification>
- `pnpm turbo typecheck` passes across all packages
- Business [id] layout wraps children in BusinessStatusProvider with status from DB
- SuspendedBanner renders at top of every sub-page when business is disabled
- Disabled business no longer returns 404 -- shows frozen dashboard instead
- All sidebar navigation links work normally when disabled
- Deploy/Redeploy/Rollback buttons disabled with "Business is suspended" tooltip
- Chat input disabled with "Business is suspended" placeholder
- Chat history remains scrollable and readable
- Agent tree '+' buttons disabled, agent pills still clickable for viewing
- Task creation blocked when business is disabled
- Health dashboard displays VPS warning badge when vps_warning is present in latest disable audit log
- Health dashboard data continues to display and auto-refresh
- Restore button in banner triggers restore with confirm dialog
- After restore, page refreshes and banner disappears
- Both "disabled" and "suspended" statuses trigger the same frozen behavior
</verification>

<success_criteria>
A disabled tenant shows a frozen dashboard with a "Suspended" banner at the top of every page. All mutation controls (deploy, create task, approve, chat, agent creation) are visually greyed out with native tooltip. The health dashboard surfaces a VPS warning indicator when VPS container stop failed during disable. Read-only content (tables, logs, conversations, deployment history) remains fully interactive and scrollable. The restore button in the banner successfully restores the tenant. All navigation links remain accessible. Full coverage of TFIX-01 (frozen dashboard with banner), TFIX-02 (VPS warning surfaced), TFIX-03 (admin panel blocks VPS interaction), and TFIX-04 (read-only frozen state). Combined with Plans 01 and 02, this completes all TFIX requirements.
</success_criteria>

<output>
After completion, create `.planning/phases/16-tenant-disable-fix-dashboard-freeze/16-03-SUMMARY.md`
</output>
