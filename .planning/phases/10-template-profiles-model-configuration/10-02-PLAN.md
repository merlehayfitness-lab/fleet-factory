---
phase: 10-template-profiles-model-configuration
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - apps/web/_components/model-selector.tsx
  - apps/web/_components/profile-editor-drawer.tsx
  - apps/web/_components/tool-profile-form.tsx
  - apps/web/_components/mcp-server-form.tsx
  - apps/web/_components/template-form.tsx
  - apps/web/_components/agent-config.tsx
  - apps/web/_actions/agent-actions.ts
  - apps/web/_actions/template-actions.ts
autonomous: true
requirements:
  - TMPL-01
  - TMPL-02
  - TMPL-03
  - TMPL-04

must_haves:
  truths:
    - "ModelSelector component renders a Select dropdown with Claude model friendly names, hiding raw API IDs"
    - "ModelSelector shows latest models by default with optional toggle to show legacy models"
    - "ModelSelector appears on BOTH the template form AND the agent config page"
    - "ProfileEditorDrawer is a fixed-position side panel (drawer) using the task-detail-panel pattern"
    - "ProfileEditorDrawer supports structured form view and raw JSON editor view with a toggle"
    - "Switching from JSON to form validates JSON first; invalid JSON blocks the switch with inline error"
    - "Switching from form to JSON serializes current form state into the JSON text"
    - "ToolProfileForm shows the allowed_tools list (add/remove) and MCP server entries"
    - "Add Tool button shows a picker with known tools from the tool-catalog"
    - "Add MCP Server button shows a picker from KNOWN_MCP_SERVERS catalog with per-server config fields"
    - "MCP server form has a 'Test Connection' button that pings http/sse servers (advisory, non-blocking)"
    - "Template form's model_profile textarea replaced with ModelSelector dropdown and Edit JSON button"
    - "Template form's tool_profile textarea replaced with summary card and Edit button that opens ProfileEditorDrawer"
    - "Agent config page's static model_profile display replaced with ModelSelector dropdown"
    - "Agent config page's static tool_profile display replaced with summary card and Edit button that opens ProfileEditorDrawer"
    - "Saving model from dropdown writes to model_profile.model key (not model_profile.modelId or other path)"
    - "Saving tool profile from drawer persists full ToolProfileShape to tool_profile column"
    - "updateAgentConfigAction accepts tool_profile and model_profile updates (already exists)"
  artifacts:
    - path: "apps/web/_components/model-selector.tsx"
      provides: "Reusable model dropdown component"
      contains: "ModelSelector"
    - path: "apps/web/_components/profile-editor-drawer.tsx"
      provides: "Side panel drawer for editing tool/model profiles with form/JSON toggle"
      contains: "ProfileEditorDrawer"
    - path: "apps/web/_components/tool-profile-form.tsx"
      provides: "Structured form for editing ToolProfileShape (tools list + MCP servers)"
      contains: "ToolProfileForm"
    - path: "apps/web/_components/mcp-server-form.tsx"
      provides: "Individual MCP server configuration form with test button"
      contains: "McpServerForm"
    - path: "apps/web/_components/template-form.tsx"
      provides: "Updated template form with ModelSelector and profile editing"
      contains: "TemplateForm"
    - path: "apps/web/_components/agent-config.tsx"
      provides: "Updated agent config with model dropdown, profile editing drawers"
      contains: "AgentConfig"
  key_links:
    - from: "apps/web/_components/model-selector.tsx"
      to: "packages/core/agent/model-constants.ts"
      via: "Imports CLAUDE_MODELS, getModelById for dropdown population"
      pattern: "CLAUDE_MODELS"
    - from: "apps/web/_components/tool-profile-form.tsx"
      to: "packages/core/agent/tool-profile-schema.ts"
      via: "Imports ToolProfileShape, KNOWN_MCP_SERVERS for form structure"
      pattern: "ToolProfileShape|KNOWN_MCP_SERVERS"
    - from: "apps/web/_components/profile-editor-drawer.tsx"
      to: "apps/web/_components/tool-profile-form.tsx"
      via: "Drawer renders ToolProfileForm in structured form mode"
      pattern: "ToolProfileForm"
    - from: "apps/web/_components/agent-config.tsx"
      to: "apps/web/_actions/agent-actions.ts"
      via: "Model dropdown change calls updateAgentConfigAction"
      pattern: "updateAgentConfigAction"
    - from: "apps/web/_components/template-form.tsx"
      to: "apps/web/_components/model-selector.tsx"
      via: "Template form embeds ModelSelector for model_profile selection"
      pattern: "ModelSelector"
---

<objective>
Build the model selector dropdown, profile editor drawer with form/JSON toggle, tool profile form, MCP server form, and update the template form and agent config page to use them.

Purpose: This plan creates the UI components for editing model and tool profiles. The ModelSelector dropdown replaces raw JSON textareas for model selection, showing friendly Claude model names. The ProfileEditorDrawer provides a side panel with structured form and raw JSON toggle for editing tool profiles. The ToolProfileForm shows the tool allowlist and MCP server list with an add-tool picker from the catalog. The MCP server form handles per-server config with advisory connectivity testing. The template form and agent config page are updated to use these new components instead of the static JSON displays.

Output: 4 new UI components, updated template-form.tsx and agent-config.tsx, updated Server Actions.
</objective>

<execution_context>
@/Users/tjbrooks/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tjbrooks/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-template-profiles-model-configuration/10-CONTEXT.md
@.planning/phases/10-template-profiles-model-configuration/10-RESEARCH.md
@.planning/phases/10-template-profiles-model-configuration/10-01-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ModelSelector, McpServerForm, ToolProfileForm, and ProfileEditorDrawer components</name>
  <files>
    apps/web/_components/model-selector.tsx
    apps/web/_components/mcp-server-form.tsx
    apps/web/_components/tool-profile-form.tsx
    apps/web/_components/profile-editor-drawer.tsx
  </files>
  <action>
    **apps/web/_components/model-selector.tsx:**
    "use client" reusable model dropdown.
    Props: `value` (string -- current model ID), `onValueChange` (callback with model ID string), `showLegacy?` (boolean, default false), `disabled?` (boolean).

    Implementation:
    - Import `CLAUDE_MODELS`, `getModelById` from `@fleet-factory/core`
    - Filter models: if `showLegacy` is false, only show models with `isLatest=true`
    - Use shadcn Select component with SelectTrigger/SelectContent/SelectItem
    - SelectTrigger shows the friendly name of the current model (from `getModelById(value)?.friendlyName` or "Select model" fallback)
    - Each SelectItem shows `model.friendlyName` as the display text, with `model.id` as the value
    - Group items by tier (optional but nice): "Powerful" (opus), "Balanced" (sonnet), "Fast" (haiku) -- or just list them flat
    - Below the main select, add a small toggle: "Show all models" checkbox that sets showLegacy to true (use internal state that defaults to the `showLegacy` prop)
    - Handle the base-ui Select pattern: `onValueChange={(val: string | null) => { if (val) onValueChange(val); }}`

    **apps/web/_components/mcp-server-form.tsx:**
    "use client" form for configuring a single MCP server.
    Props: `server` (McpServerConfig), `onChange` (callback with updated McpServerConfig), `onRemove` (callback), `onTest?` (callback -- optional test action).

    Layout (within a bordered card):
    - Name: text Input
    - URL/Command: text Input (label changes based on transport: "Command" for stdio, "URL" for http/sse)
    - Transport: Select with stdio/http/sse options
    - Enabled: native HTML checkbox toggle
    - Environment Variables section: key-value pairs with add/remove. Each row: key Input + value Input + remove button. "Add Variable" button below.
    - Footer row: "Test Connection" button (only enabled for http/sse transport, calls onTest) and "Remove Server" button (calls onRemove with confirmation)
    - Test result: show inline text "Reachable" (green) or "Unreachable: {error}" (red) or nothing if not tested

    Keep the component under 150 lines. Each env var row can be a simple inline div.

    **apps/web/_components/tool-profile-form.tsx:**
    "use client" structured form for ToolProfileShape.
    Props: `profile` (ToolProfileShape), `onChange` (callback with updated ToolProfileShape), `businessId` (string).

    Layout:
    - **Allowed Tools** section:
      - List of tool name badges with X buttons to remove
      - "Add Tool" button opens a dropdown/popover listing known tools from the per-department tool catalog (import tool names from core tool-catalog or just use the allowed_tools already in profile + a free-text input)
      - Simple approach: text Input for tool name + "Add" button. Pre-populate a dropdown with known tool names from DEPARTMENT_DEFAULT_TOOL_PROFILES (all departments combined) for autocomplete feel.
      - Special badge for `"*"` (All tools) -- if present, show "All tools allowed" with an option to restrict

    - **MCP Servers** section:
      - List of McpServerForm components for each server in `profile.mcp_servers`
      - "Add MCP Server" button opens a picker dialog showing KNOWN_MCP_SERVERS catalog
      - Picker: simple list of known servers with name, description, category badge. Clicking one adds it to the profile with default config fields populated. "Custom Server" option at the bottom creates a blank entry.
      - Empty state: "No MCP servers configured"

    - Each change calls `onChange` with the updated ToolProfileShape

    State: managed internally via local state initialized from `profile` prop. Call `onChange` on every meaningful change (add/remove tool, add/remove/update MCP server).

    MCP Server test: call a Server Action `testMcpConnectionAction(url, transport)` that uses `validateMcpServerUrl` from core. Show inline result.

    **apps/web/_components/profile-editor-drawer.tsx:**
    "use client" fixed-position side panel for profile editing.
    Props: `isOpen` (boolean), `onClose` (callback), `title` (string), `profile` (ToolProfileShape or Record<string,unknown>), `onSave` (callback with updated profile), `businessId` (string).

    Layout -- matches the `task-detail-panel.tsx` pattern:
    ```
    {isOpen && (
      <div className="fixed inset-0 z-50 flex justify-end">
        <div className="absolute inset-0 bg-black/10 backdrop-blur-xs" onClick={onClose} />
        <div className="relative z-10 flex w-full max-w-xl flex-col bg-popover shadow-lg ring-1 ring-foreground/10">
          {/* Header */}
          {/* Content */}
          {/* Footer */}
        </div>
      </div>
    )}
    ```

    Header:
    - Title (e.g., "Edit Tool Profile")
    - Toggle: "Form" / "JSON" view mode buttons (two small buttons, active one highlighted)
    - Close button (X icon)

    Content (scrollable):
    - **Form view**: Renders `ToolProfileForm` with the current profile state
    - **JSON view**: Textarea (full width, monospace, min-h-64) with raw JSON text
      - JSON error message shown inline below textarea if parse fails
      - Text initialized from `JSON.stringify(profile, null, 2)` on first open

    Toggle logic:
    - Form -> JSON: serialize current form state to JSON text
    - JSON -> Form: try JSON.parse. If valid, apply to form state. If invalid, show error and block switch.

    Footer:
    - "Save" button (calls `onSave` with current profile from whichever view is active; validates JSON first if in JSON mode)
    - "Cancel" button (calls `onClose`)
    - Saving state with disabled button

    State management:
    - `viewMode: "form" | "json"` (default "form")
    - `formProfile: ToolProfileShape` (initialized from props, updated by ToolProfileForm onChange)
    - `jsonText: string` (initialized from JSON.stringify of profile)
    - `jsonError: string | null`

    Run `pnpm turbo typecheck` to verify.
  </action>
  <verify>
    Run `pnpm turbo typecheck` (no type errors).
    Verify ModelSelector renders Select with friendly model names and hides API IDs.
    Verify ModelSelector filters to latest models by default.
    Verify McpServerForm shows name, url, transport, env vars, test button, remove button.
    Verify ToolProfileForm shows allowed_tools badges and MCP server entries.
    Verify "Add MCP Server" shows KNOWN_MCP_SERVERS catalog picker.
    Verify ProfileEditorDrawer opens as fixed-position side panel with form/JSON toggle.
    Verify JSON->Form switch validates JSON and blocks on parse error.
    Verify Form->JSON switch serializes current form state.
  </verify>
  <done>
    ModelSelector dropdown shows friendly Claude model names with latest/legacy toggle. McpServerForm handles per-server config with test connection button. ToolProfileForm manages allowed_tools and MCP servers with add-from-catalog picker. ProfileEditorDrawer provides fixed-position side panel with form/JSON bidirectional toggle and validation.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update template form and agent config page with model dropdown and profile editing</name>
  <files>
    apps/web/_components/template-form.tsx
    apps/web/_components/agent-config.tsx
    apps/web/_actions/agent-actions.ts
    apps/web/_actions/template-actions.ts
  </files>
  <action>
    **apps/web/_components/template-form.tsx:**
    Update the existing template form to replace raw JSON textareas with proper UI:

    1. **Model Profile section** -- Replace the model_profile Textarea with:
       - Import `ModelSelector` from `./model-selector`
       - Import `DEPARTMENT_DEFAULT_MODELS`, `getModelFriendlyName` from `@fleet-factory/core`
       - Add state: `modelId` initialized from `template?.model_profile?.model` (cast as string) or `DEPARTMENT_DEFAULT_MODELS[departmentType] ?? "claude-sonnet-4-6"`
       - Render `ModelSelector` with value={modelId} and onValueChange that updates modelId state AND calls `setValue("model_profile", { model: newModelId })`
       - When department_type changes in the form, if the model is still the default for the previous department, auto-switch to the new department's default model
       - Below the selector, add a small "Edit full JSON" link/button that opens a ProfileEditorDrawer for model_profile (for power users who want to add temperature, etc.)

    2. **Tool Profile section** -- Replace the tool_profile Textarea with:
       - Import `ProfileEditorDrawer` from `./profile-editor-drawer`
       - Import `ToolProfileShape`, `EMPTY_TOOL_PROFILE`, `DEPARTMENT_DEFAULT_TOOL_PROFILES` from `@fleet-factory/core`
       - Add state: `toolProfile` (ToolProfileShape) initialized from template?.tool_profile (parsed as ToolProfileShape) or DEPARTMENT_DEFAULT_TOOL_PROFILES[departmentType] or EMPTY_TOOL_PROFILE
       - Show a summary card: "{N} tools allowed, {M} MCP servers" with an "Edit" button
       - "Edit" button opens ProfileEditorDrawer with the current toolProfile
       - On save from drawer: update toolProfile state AND call `setValue("tool_profile", updatedProfile)`
       - When department_type changes, if tool profile is still the default for the previous department, auto-switch to the new department's default

    3. Update the onSubmit handler to serialize model_profile and tool_profile from the new state instead of the old textarea values. Ensure `model_profile` is serialized as `{ model: modelId }` (plus any other keys from JSON editor if used).

    **apps/web/_components/agent-config.tsx:**
    Update the agent config page to replace static JSON displays with interactive editing:

    1. **Model Profile card** -- Replace the static JSON display:
       - Import `ModelSelector` from `./model-selector`
       - Import `getModelFriendlyName` from `@fleet-factory/core`
       - Extract current model ID: `(agent.model_profile as { model?: string })?.model ?? ""`
       - Replace the pre/code block with a ModelSelector dropdown
       - On model change: call `updateAgentConfigAction(agent.id, businessId, { model_profile: { ...agent.model_profile, model: newModelId } })`
       - Show a toast after save: "Model updated to {friendlyName}"
       - Add saving state to disable dropdown during save

    2. **Tool Profile card** -- Replace the static JSON display:
       - Import `ProfileEditorDrawer` from `./profile-editor-drawer`
       - Import `ToolProfileShape` from `@fleet-factory/core`
       - Parse current tool profile as ToolProfileShape
       - Replace the pre/code block with a summary card:
         - Show tool count: "{N} tools allowed" (or "All tools" if includes "*")
         - Show MCP count: "{M} MCP servers configured"
         - List first 3-5 tool names as badges
         - "Edit" button opens ProfileEditorDrawer
       - On save from drawer: call `updateAgentConfigAction(agent.id, businessId, { tool_profile: updatedProfile })`
       - Show toast with redeploy prompt: `toast("Tool profile updated", { action: { label: "Redeploy", onClick: () => router.push(\`/businesses/${businessId}/deployments\`) } })`

    3. **Redeploy toast after model change**: After saving model changes, show the same redeploy toast pattern:
       `toast("Model updated to {friendlyName}", { action: { label: "Redeploy", onClick: ... } })`

    4. Keep the existing Template Differences section working -- it still compares agent vs template values using `isEqual()`. No changes needed there since the underlying data shape stays as JSONB objects.

    **apps/web/_actions/agent-actions.ts:**
    The existing `updateAgentConfigAction` already accepts `tool_profile` and `model_profile` in its config parameter. Verify this works with the new ToolProfileShape objects (should be fine since it accepts `Record<string, unknown>`).

    Add a new Server Action for MCP server connectivity testing:
    ```typescript
    export async function testMcpConnectionAction(
      url: string,
      transport: string,
    ): Promise<{ reachable: boolean; error?: string }> {
      const supabase = await createServerClient();
      const { data: { user } } = await supabase.auth.getUser();
      if (!user) return { reachable: false, error: "Not authenticated" };

      // Import validateMcpServerUrl from core
      const { validateMcpServerUrl } = await import("@fleet-factory/core/server");
      return validateMcpServerUrl(url, transport);
    }
    ```

    **apps/web/_actions/template-actions.ts:**
    Review the existing createTemplateAction and updateTemplateAction. They should already handle `tool_profile` and `model_profile` from FormData. The template form now sends structured objects instead of raw JSON strings, so verify the actions properly parse/handle the data. If needed, update to accept the profile data directly rather than from FormData string parsing.

    Run `pnpm turbo typecheck` to verify.
  </action>
  <verify>
    Run `pnpm turbo typecheck` (no type errors).
    Verify template form shows ModelSelector instead of model_profile textarea.
    Verify template form shows tool profile summary card with Edit button instead of tool_profile textarea.
    Verify model selection writes to model_profile.model key.
    Verify ProfileEditorDrawer opens from both template form and agent config.
    Verify agent config page shows ModelSelector dropdown for model.
    Verify agent config page shows tool profile summary with Edit button.
    Verify model change on agent config calls updateAgentConfigAction with correct payload.
    Verify tool profile save from drawer calls updateAgentConfigAction.
    Verify redeploy toast appears after model/tool profile changes on agent config.
    Verify testMcpConnectionAction exists and calls validateMcpServerUrl.
    Verify template form department change auto-switches default model and tool profile.
  </verify>
  <done>
    Template form updated: ModelSelector replaces model_profile textarea, tool profile summary card with Edit button replaces tool_profile textarea, department change auto-updates defaults. Agent config updated: ModelSelector dropdown for model selection, tool profile summary with Edit drawer, redeploy toast after changes. MCP connection test Server Action added. All model writes target model_profile.model key for backward compatibility.
  </done>
</task>

</tasks>

<verification>
- `pnpm turbo typecheck` passes
- ModelSelector shows friendly Claude model names (e.g., "Claude Opus 4.6") not raw API IDs
- ModelSelector filters to latest models by default; "Show all" reveals legacy models
- ProfileEditorDrawer opens as side panel with form/JSON toggle and bidirectional data sync
- JSON parse errors block form/JSON switch with inline error message
- ToolProfileForm shows tool badges (add/remove) and MCP server cards with config
- Add MCP Server shows KNOWN_MCP_SERVERS catalog picker
- MCP server "Test Connection" pings http/sse URLs (advisory, non-blocking save)
- Template form uses ModelSelector for model and ProfileEditorDrawer for tools
- Agent config uses ModelSelector for model and ProfileEditorDrawer for tools
- Model changes write to model_profile.model key (verified by checking updateAgentConfigAction payload)
- Tool profile changes write full ToolProfileShape to tool_profile column
- Redeploy toast appears after model or tool profile changes on agent config
- Department type change on template form auto-switches model and tool defaults
- Template Differences section on agent config still works correctly
</verification>

<success_criteria>
Model dropdown on both template form and agent config (TMPL-03). Structured form editing with raw JSON toggle via side panel drawer (TMPL-04). Model writes to model_profile.model key maintaining backward compat with runtime/deployment. Tool profile editable via structured form with tool allowlist and MCP server management. Add-tool and add-MCP-server catalog pickers. Redeploy prompt after agent config changes. Full coverage: TMPL-01, TMPL-02, TMPL-03, TMPL-04.
</success_criteria>

<output>
After completion, create `.planning/phases/10-template-profiles-model-configuration/10-02-SUMMARY.md`
</output>
