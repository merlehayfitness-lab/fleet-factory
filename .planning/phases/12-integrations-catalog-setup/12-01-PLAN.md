---
phase: 12-integrations-catalog-setup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/integrations/catalog.ts
  - packages/core/integrations/service.ts
  - packages/core/integrations/index.ts
  - packages/core/index.ts
  - packages/db/schema/038_integrations_department_scope.sql
  - packages/db/schema/_combined_schema.sql
  - apps/web/public/integrations/hubspot.svg
  - apps/web/public/integrations/salesforce.svg
  - apps/web/public/integrations/pipedrive.svg
  - apps/web/public/integrations/sendgrid.svg
  - apps/web/public/integrations/mailgun.svg
  - apps/web/public/integrations/ses.svg
  - apps/web/public/integrations/zendesk.svg
  - apps/web/public/integrations/freshdesk.svg
  - apps/web/public/integrations/intercom.svg
  - apps/web/public/integrations/google-calendar.svg
  - apps/web/public/integrations/outlook-calendar.svg
  - apps/web/public/integrations/calendly.svg
  - apps/web/public/integrations/slack.svg
  - apps/web/public/integrations/teams.svg
  - apps/web/public/integrations/discord.svg
  - apps/web/_components/integration-catalog-dialog.tsx
  - apps/web/_components/catalog-target-picker.tsx
  - apps/web/_actions/integration-actions.ts
  - apps/web/app/(dashboard)/businesses/[id]/integrations/page.tsx
  - apps/web/_components/integrations-overview.tsx
  - apps/web/_components/integration-config-card.tsx
  - apps/web/_components/agent-integrations.tsx
autonomous: true
requirements:
  - INTG-ENH-01
  - INTG-ENH-02
  - INTG-ENH-04

must_haves:
  truths:
    - "INTEGRATION_CATALOG is a static TypeScript array of 15 CatalogEntry objects across 5 categories (crm, email, helpdesk, calendar, messaging)"
    - "Each CatalogEntry has id, name, description, category (IntegrationType), provider, logoUrl, isReal, defaultConfig fields"
    - "Migration 038 adds department_id (uuid FK), setup_instructions (text), and name (text) columns to integrations table"
    - "Migration 038 drops old idx_integrations_business_agent_type and creates two partial unique indexes for agent-level and department-level"
    - "Integration catalog dialog is a multi-step Dialog: Step 1 browse/search, Step 2 assign targets"
    - "Catalog dialog has a search bar that filters integrations by name and category grouping below"
    - "Each catalog entry displays logo, name, and short description"
    - "CatalogTargetPicker shows departments AND agents together with multi-select checkboxes"
    - "Department assignment sets department_id on integration record with agent_id NULL (lazy inheritance)"
    - "Agent assignment sets agent_id on integration record with department_id NULL (existing behavior)"
    - "addCatalogIntegrationAction creates multiple integration records in one call for multi-target assignment"
    - "Category auto-populates from catalog entry's category field (INTG-ENH-04)"
    - "Integrations page has 'Add Integration' button that opens the catalog dialog"
    - "IntegrationsOverview empty state references 'Add Integration' button instead of agent detail page"
    - "IntegrationConfigCard handles department-level integrations (shows department name when agent_id is NULL)"
    - "AgentIntegrations replaces per-type 'Add Mock' buttons with 'Add from Catalog' button"
    - "15 SVG brand logo files exist in apps/web/public/integrations/"
  artifacts:
    - path: "packages/core/integrations/catalog.ts"
      provides: "Static integration catalog data with 15 entries"
      exports: ["CatalogEntry", "INTEGRATION_CATALOG", "getCatalogByCategory", "getCatalogEntry"]
    - path: "packages/db/schema/038_integrations_department_scope.sql"
      provides: "Migration adding department_id, setup_instructions, name columns and partial unique indexes"
      contains: "ALTER TABLE public.integrations"
    - path: "apps/web/_components/integration-catalog-dialog.tsx"
      provides: "Multi-step catalog browsing dialog with search and category groups"
      contains: "IntegrationCatalogDialog"
    - path: "apps/web/_components/catalog-target-picker.tsx"
      provides: "Multi-select picker for departments and agents as assignment targets"
      contains: "CatalogTargetPicker"
  key_links:
    - from: "packages/core/integrations/catalog.ts"
      to: "apps/web/_components/integration-catalog-dialog.tsx"
      via: "Dialog imports INTEGRATION_CATALOG for browsing display"
      pattern: "INTEGRATION_CATALOG"
    - from: "apps/web/_components/integration-catalog-dialog.tsx"
      to: "apps/web/_actions/integration-actions.ts"
      via: "Dialog calls addCatalogIntegrationAction on target selection"
      pattern: "addCatalogIntegrationAction"
    - from: "packages/db/schema/038_integrations_department_scope.sql"
      to: "packages/core/integrations/service.ts"
      via: "Service queries updated to handle department_id column"
      pattern: "department_id"
---

<objective>
Build the integration catalog data model, database migration for department-level assignment, browsable catalog dialog, multi-target assignment picker, and update all existing integration UI components.

Purpose: This plan creates the static catalog of 14 integrations with metadata (name, description, logo, category), a DB migration adding department_id/setup_instructions/name columns with partial unique indexes, a multi-step Dialog for browsing and selecting integrations, a target picker for multi-assigning to departments and agents, and updates to all existing integration pages/components. Category auto-populates from the selected catalog entry. The "Add Mock" per-type buttons are replaced with a centralized "Add from Catalog" flow.

Output: 1 new core module (catalog.ts), migration 038, 15 SVG logos, 2 new UI components (catalog dialog + target picker), updated integration service, updated actions, updated pages/components.
</objective>

<execution_context>
@/Users/tjbrooks/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tjbrooks/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-integrations-catalog-setup/12-CONTEXT.md
@.planning/phases/12-integrations-catalog-setup/12-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Catalog data module, schema migration, integration service updates, SVG logos, and core exports</name>
  <files>
    packages/core/integrations/catalog.ts
    packages/core/integrations/service.ts
    packages/core/integrations/index.ts
    packages/core/index.ts
    packages/db/schema/038_integrations_department_scope.sql
    packages/db/schema/_combined_schema.sql
    apps/web/public/integrations/hubspot.svg
    apps/web/public/integrations/salesforce.svg
    apps/web/public/integrations/pipedrive.svg
    apps/web/public/integrations/sendgrid.svg
    apps/web/public/integrations/mailgun.svg
    apps/web/public/integrations/ses.svg
    apps/web/public/integrations/zendesk.svg
    apps/web/public/integrations/freshdesk.svg
    apps/web/public/integrations/intercom.svg
    apps/web/public/integrations/google-calendar.svg
    apps/web/public/integrations/outlook-calendar.svg
    apps/web/public/integrations/calendly.svg
    apps/web/public/integrations/slack.svg
    apps/web/public/integrations/teams.svg
    apps/web/public/integrations/discord.svg
  </files>
  <action>
    **packages/core/integrations/catalog.ts:**
    Create the static integration catalog with types and data:

    ```typescript
    export interface CatalogEntry {
      id: string;              // Unique key: "slack", "hubspot", etc.
      name: string;            // Display name: "Slack", "HubSpot"
      description: string;     // Short description for catalog browsing
      category: IntegrationType; // Maps to existing 5 types
      provider: string;        // Provider key for adapter: "slack", "hubspot"
      logoUrl: string;         // Path to SVG: "/integrations/slack.svg"
      isReal: boolean;         // Whether this has a real connection (vs mock)
      defaultConfig?: Record<string, unknown>;
    }
    ```

    Populate `INTEGRATION_CATALOG: CatalogEntry[]` with 15 entries (3 per category):
    - CRM: hubspot ("HubSpot", "Customer relationship management and sales pipeline"), salesforce ("Salesforce", "Enterprise CRM platform"), pipedrive ("Pipedrive", "Sales pipeline and deal management")
    - Email: sendgrid ("SendGrid", "Transactional and marketing email delivery"), mailgun ("Mailgun", "Email sending and routing API"), ses ("Amazon SES", "Scalable cloud email service")
    - Helpdesk: zendesk ("Zendesk", "Customer support ticketing and help center"), freshdesk ("Freshdesk", "Customer support and helpdesk platform"), intercom ("Intercom", "Customer messaging and support platform")
    - Calendar: google-calendar ("Google Calendar", "Scheduling and calendar management"), outlook-calendar ("Outlook Calendar", "Microsoft calendar and scheduling"), calendly ("Calendly", "Automated meeting scheduling and booking")
    - Messaging: slack ("Slack", "Team messaging and collaboration"), teams ("Microsoft Teams", "Team communication and collaboration"), discord ("Discord", "Community chat and voice platform")

    All entries have `isReal: false` except hubspot and sendgrid which have `isReal: true` (these are the two proof-of-concept real connections per CONTEXT).
    Set `logoUrl` to `/integrations/{id}.svg` for each entry.

    Add helpers:
    - `getCatalogByCategory(): Record<IntegrationType, CatalogEntry[]>` -- group catalog by category
    - `getCatalogEntry(id: string): CatalogEntry | undefined` -- find by id

    **packages/db/schema/038_integrations_department_scope.sql:**
    Create migration:
    ```sql
    -- 038: Add department-level integration support and catalog metadata
    ALTER TABLE public.integrations ADD COLUMN IF NOT EXISTS department_id uuid REFERENCES public.departments ON DELETE CASCADE;
    ALTER TABLE public.integrations ADD COLUMN IF NOT EXISTS setup_instructions text;
    ALTER TABLE public.integrations ADD COLUMN IF NOT EXISTS name text;

    CREATE INDEX IF NOT EXISTS idx_integrations_department ON public.integrations (department_id);

    -- Replace old unique constraint with partial indexes
    DROP INDEX IF EXISTS idx_integrations_business_agent_type;

    -- Agent-level: one integration per type per agent per business
    CREATE UNIQUE INDEX IF NOT EXISTS idx_integrations_agent_unique
      ON public.integrations (business_id, agent_id, type) WHERE agent_id IS NOT NULL;

    -- Department-level: one integration per type per department per business
    CREATE UNIQUE INDEX IF NOT EXISTS idx_integrations_department_unique
      ON public.integrations (business_id, department_id, type) WHERE department_id IS NOT NULL;
    ```

    Append to `_combined_schema.sql` with `-- 038: Integrations department scope` section comment.

    **packages/core/integrations/service.ts:**
    Update the Integration interface to include optional `department_id`, `setup_instructions`, `name` fields.
    Add `IntegrationWithDepartment` interface extending IntegrationWithAgent with `departments: { id: string; name: string } | null`.

    Add new functions:
    - `getIntegrationsForDepartment(supabase, departmentId): Promise<Integration[]>` -- fetch integrations where department_id matches
    - `bulkCreateIntegrations(supabase, businessId, entries: Array<{ agentId?: string; departmentId?: string; type: string; provider: string; name: string; config?: Record<string,unknown> }>): Promise<Integration[]>` -- check-then-insert pattern (query existing, skip duplicates, insert new). Use single `.insert()` call with array. Audit log best-effort.
    - `getEffectiveIntegrationsForAgent(supabase, agentId, departmentId, businessId): Promise<Integration[]>` -- returns agent-level + department-level inherited integrations

    Update `getIntegrationsForBusiness` to also join with departments: `.select("*, agents(id, name), departments(id, name)")`. Return type becomes `IntegrationWithDepartment[]`.

    Update `upsertIntegration` to accept optional `departmentId` and `name` params. When departmentId is provided and agentId is not, create department-level record. Adjust the upsert `onConflict` handling: since partial indexes don't work cleanly with Supabase upsert, use check-then-insert pattern (query for existing record with matching business_id + agent_id/department_id + type, then INSERT or UPDATE accordingly).

    **packages/core/integrations/index.ts:**
    Add re-export: `export { INTEGRATION_CATALOG, getCatalogByCategory, getCatalogEntry } from "./catalog";`
    Export the CatalogEntry type: `export type { CatalogEntry } from "./catalog";`

    **packages/core/index.ts:**
    Add exports for catalog data:
    ```typescript
    // Integration catalog (client-safe)
    export type { CatalogEntry } from "./integrations/catalog";
    export { INTEGRATION_CATALOG, getCatalogByCategory, getCatalogEntry } from "./integrations/catalog";
    ```

    **SVG Logo Files:**
    Create 15 simple SVG files in `apps/web/public/integrations/`. Each should be a minimal brand-colored SVG icon at 40x40 viewBox. Use the first letter of the brand name in a colored circle as a clean fallback that works universally:
    - hubspot.svg: "H" in #FF7A59 (HubSpot orange)
    - salesforce.svg: "S" in #00A1E0 (Salesforce blue)
    - pipedrive.svg: "P" in #017737 (Pipedrive green)
    - sendgrid.svg: "S" in #1A82E2 (SendGrid blue)
    - mailgun.svg: "M" in #F06B54 (Mailgun red)
    - ses.svg: "A" in #FF9900 (AWS orange)
    - zendesk.svg: "Z" in #03363D (Zendesk dark)
    - freshdesk.svg: "F" in #2AB467 (Freshdesk green)
    - intercom.svg: "I" in #286EFA (Intercom blue)
    - google-calendar.svg: "G" in #4285F4 (Google blue)
    - outlook-calendar.svg: "O" in #0078D4 (Microsoft blue)
    - calendly.svg: "C" in #006BFF (Calendly blue)
    - slack.svg: "S" in #4A154B (Slack purple)
    - teams.svg: "T" in #6264A7 (Teams purple)
    - discord.svg: "D" in #5865F2 (Discord blurple)

    Each SVG follows this template (adjust letter and color):
    ```svg
    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 40 40" width="40" height="40">
      <circle cx="20" cy="20" r="20" fill="{color}"/>
      <text x="20" y="20" text-anchor="middle" dominant-baseline="central" fill="white" font-family="system-ui,sans-serif" font-size="20" font-weight="600">{letter}</text>
    </svg>
    ```

    Run `pnpm turbo typecheck` to verify.
  </action>
  <verify>
    Run `pnpm turbo typecheck` (no type errors).
    Verify INTEGRATION_CATALOG has 15 entries across 5 categories.
    Verify getCatalogByCategory returns entries grouped by IntegrationType.
    Verify getCatalogEntry("slack") returns the Slack entry.
    Verify migration 038 adds department_id, setup_instructions, name columns.
    Verify migration 038 creates two partial unique indexes.
    Verify bulkCreateIntegrations exists in service with check-then-insert pattern.
    Verify getEffectiveIntegrationsForAgent queries both agent and department integrations.
    Verify getIntegrationsForBusiness joins with departments.
    Verify all 15 SVG files exist in apps/web/public/integrations/.
    Verify CatalogEntry and INTEGRATION_CATALOG exported from packages/core/index.ts.
  </verify>
  <done>
    Integration catalog module with 15 entries (3 CRM, 3 email, 3 helpdesk, 3 calendar, 3 messaging). Migration 038 adds department_id, setup_instructions, name columns with partial unique indexes replacing old constraint. Service updated with bulkCreateIntegrations, getEffectiveIntegrationsForAgent, getIntegrationsForDepartment, and department-aware business query. 15 SVG logo files created. All types and catalog data exported from core.
  </done>
</task>

<task type="auto">
  <name>Task 2a: Catalog dialog, target picker, and server action</name>
  <files>
    apps/web/_components/integration-catalog-dialog.tsx
    apps/web/_components/catalog-target-picker.tsx
    apps/web/_actions/integration-actions.ts
  </files>
  <action>
    **apps/web/_components/catalog-target-picker.tsx:**
    "use client" component for multi-selecting assignment targets.
    Props: `departments: Array<{ id: string; name: string; type: string }>`, `agents: Array<{ id: string; name: string; department_id: string }>`, `selectedDepartments: string[]`, `selectedAgents: string[]`, `onDepartmentsChange: (ids: string[]) => void`, `onAgentsChange: (ids: string[]) => void`.

    Layout:
    - Group by department. Each department section shows:
      - Native HTML checkbox for the department itself (label: "Entire {name} Department", styled with border-left-4 in department color)
      - Indented list of agents under that department, each with native HTML checkbox
    - When department checkbox is checked, it selects the department ID (not individual agents)
    - Agent checkboxes select individual agent IDs
    - Visual: department rows use slightly bolder styling, agent rows indented with smaller text
    - Show selected count at bottom: "{N} targets selected"

    **apps/web/_components/integration-catalog-dialog.tsx:**
    "use client" multi-step dialog for browsing and adding integrations.
    Props: `businessId: string`, `departments: Array<{ id: string; name: string; type: string }>`, `agents: Array<{ id: string; name: string; department_id: string }>`, `trigger: React.ReactNode` (the button that opens the dialog), `preSelectedAgentId?: string` (for agent-scoped opening).

    State: `step: 1 | 2 | 3`, `selectedEntry: CatalogEntry | null`, `searchQuery: string`, `selectedDepartments: string[]`, `selectedAgents: string[]`, `isCreating: boolean`, `createdIntegrationIds: string[]`.

    Step 1 -- Browse & Select:
    - Search Input at top filtering integrations by name (case-insensitive)
    - Below: integrations grouped by category using getCatalogByCategory()
    - Category headers: "CRM", "Email", "Helpdesk", "Calendar", "Messaging"
    - Each entry row: `<Image src={entry.logoUrl} width={32} height={32} />`, name (bold), description (muted)
    - Clicking a row sets selectedEntry and advances to step 2
    - If preSelectedAgentId is set, show note at top: "Adding integration for {agent name}"

    Step 2 -- Assign Targets:
    - Header: "Add {selectedEntry.name}" with logo
    - If preSelectedAgentId: pre-select that agent, show picker but with pre-selection
    - Otherwise: render CatalogTargetPicker with full department/agent list
    - "Add Integration" button (disabled if no targets selected)
    - On click: call addCatalogIntegrationAction with selectedEntry + targets, set isCreating=true
    - On success: store returned integration IDs, advance to step 3
    - On error: show toast error

    Step 3 -- Confirmation (placeholder for plan 12-02's AI instructions):
    - Show success message: "{selectedEntry.name} added to {N} targets"
    - List of targets with checkmarks
    - "Done" button closes dialog
    - Note: "Setup instructions will appear here" (placeholder for 12-02)

    Dialog uses controlled open state. Reset all state when dialog closes.

    **apps/web/_actions/integration-actions.ts:**
    Add new server action:
    ```typescript
    export async function addCatalogIntegrationAction(
      businessId: string,
      catalogEntryId: string,
      departmentIds: string[],
      agentIds: string[],
    ): Promise<{ integrationIds?: string[]; error?: string }>
    ```

    Implementation:
    - Auth check (redirect if no user)
    - Import getCatalogEntry from @fleet-factory/core
    - Look up catalog entry by ID; return error if not found
    - Build entries array: for each departmentId create `{ departmentId, type: entry.category, provider: entry.provider, name: entry.name }`; for each agentId create `{ agentId, type: entry.category, provider: entry.provider, name: entry.name }`
    - Call bulkCreateIntegrations from core service
    - revalidatePath for the integrations page
    - Return integration IDs

    Update `getBusinessIntegrationsAction` to also join departments: `.select("*, agents(id, name), departments(id, name)")`.

    Run `pnpm turbo typecheck` to verify.
  </action>
  <verify>
    Run `pnpm turbo typecheck` (no type errors).
    Verify IntegrationCatalogDialog renders as multi-step dialog with browse and assign steps.
    Verify search filters catalog entries by name.
    Verify category grouping shows all 5 categories with entries.
    Verify CatalogTargetPicker shows departments and agents with checkboxes.
    Verify addCatalogIntegrationAction creates records for both department and agent targets.
    Verify category is auto-set from catalog entry (never manually selected).
  </verify>
  <done>
    IntegrationCatalogDialog with multi-step flow (browse with search/categories, assign to departments/agents). CatalogTargetPicker with multi-select showing departments and agents grouped. addCatalogIntegrationAction for bulk creation with category auto-populated from catalog entry.
  </done>
</task>

<task type="auto">
  <name>Task 2b: Integrations page, overview, config card, and agent-integrations updates</name>
  <files>
    apps/web/app/(dashboard)/businesses/[id]/integrations/page.tsx
    apps/web/_components/integrations-overview.tsx
    apps/web/_components/integration-config-card.tsx
    apps/web/_components/agent-integrations.tsx
  </files>
  <action>
    **apps/web/app/(dashboard)/businesses/[id]/integrations/page.tsx:**
    - Add fetch for departments: `const { data: departments } = await supabase.from("departments").select("id, name, type").eq("business_id", businessId).order("name");`
    - Add fetch for agents with department_id: update agents query to include department_id in select
    - Import IntegrationCatalogDialog
    - Add "Add Integration" Button as the trigger for the dialog in the page header area (next to the h1 title)
    - Pass departments, agents, businessId to the dialog

    **apps/web/_components/integrations-overview.tsx:**
    - Update empty state text: change "Visit an agent's detail page to add integrations." to "Use the 'Add Integration' button above to get started."
    - Update Integration interface to include optional `department_id?: string` and `departments?: { id: string; name: string } | null`
    - In summary stats, count department-level integrations separately or note them
    - In the grouped-by-type display, department-level integrations (where agent_id is NULL) should show with "Department: {name}" instead of an agent link

    **apps/web/_components/integration-config-card.tsx:**
    - Update Integration interface to include optional `department_id?: string`, `name?: string`, `departments?: { id: string; name: string } | null`
    - When `agent_id` is NULL and `departments` is set, show department name with a department icon instead of agent link
    - When `name` is set (catalog name like "Slack"), show it below the agent/department name as the provider display
    - Keep existing functionality for agent-level integration cards

    **apps/web/_components/agent-integrations.tsx:**
    - Import IntegrationCatalogDialog
    - Replace the per-type "Add Mock" buttons in the unconfigured integration sections
    - Add a single "Add from Catalog" button at the top of the component that opens IntegrationCatalogDialog with `preSelectedAgentId={agentId}`
    - The button needs departments and agents props -- add these to AgentIntegrationsProps
    - Keep the existing configured integration cards and provider dropdown as-is
    - Remove the per-section handleAddMock function and "Add Mock" button from the unconfigured state (replace with text like "Not configured -- use Add from Catalog above")

    Run `pnpm turbo typecheck` to verify.
  </action>
  <verify>
    Run `pnpm turbo typecheck` (no type errors).
    Verify integrations page has "Add Integration" button opening the dialog.
    Verify integrations page fetches and passes departments to the dialog.
    Verify IntegrationsOverview empty state references "Add Integration" button.
    Verify IntegrationsOverview shows "Department: {name}" for department-level integrations.
    Verify IntegrationConfigCard shows department name for department-level integrations.
    Verify AgentIntegrations has "Add from Catalog" button replacing per-type "Add Mock".
  </verify>
  <done>
    Integrations page updated with "Add Integration" button and department data fetching. IntegrationsOverview handles department-level integrations with department name display. IntegrationConfigCard shows department name when applicable. AgentIntegrations uses "Add from Catalog" replacing per-type mock buttons.
  </done>
</task>

</tasks>

<verification>
- `pnpm turbo typecheck` passes
- INTEGRATION_CATALOG has 15 entries: 3 CRM, 3 email, 3 helpdesk, 3 calendar, 3 messaging
- Each CatalogEntry has id, name, description, category, provider, logoUrl, isReal, defaultConfig
- Migration 038 adds department_id, setup_instructions, name columns to integrations
- Migration 038 drops old unique index and creates two partial unique indexes (agent-level + department-level)
- 15 SVG logo files exist in apps/web/public/integrations/
- "Add Integration" button on integrations page opens catalog dialog
- Catalog dialog Step 1: search bar + category-grouped entries with logos, names, descriptions
- Catalog dialog Step 2: CatalogTargetPicker with departments + agents, multi-select
- Clicking "Add Integration" in Step 2 calls addCatalogIntegrationAction and creates records
- Department assignment: department_id set, agent_id NULL (lazy inheritance)
- Agent assignment: agent_id set, department_id NULL (existing behavior)
- Category auto-populates from CatalogEntry.category (INTG-ENH-04)
- IntegrationsOverview shows department name for department-level integrations
- AgentIntegrations uses "Add from Catalog" instead of per-type "Add Mock" buttons
- IntegrationConfigCard handles both agent-level and department-level display
</verification>

<success_criteria>
Browsable integration catalog dialog with 15 integrations across 5 categories (3 per category), search, and brand logos (INTG-ENH-01). Multi-target assignment to departments and agents with department-level lazy inheritance (INTG-ENH-02). Category auto-population from catalog entry selection (INTG-ENH-04). Schema migration supports department-level integrations. All existing integration UI updated to use catalog flow.
</success_criteria>

<output>
After completion, create `.planning/phases/12-integrations-catalog-setup/12-01-SUMMARY.md`
</output>
</content>
</invoke>