---
phase: 18-enhanced-business-wizard-agent-hierarchy
plan: 02
type: execute
wave: 2
depends_on:
  - 18-01
files_modified:
  - apps/web/_components/department-tree-select.tsx
  - apps/web/_components/create-business-wizard.tsx
autonomous: true
requirements:
  - WIZ-03
  - HIER-03
must_haves:
  truths:
    - "Department tree nodes show name only in the tree; role, budget, model, and description appear on hover tooltip"
    - "Deselecting a department head auto-deselects all its child specialist agents"
    - "CEO node is always selected and cannot be deselected"
    - "API key providers list dynamically updates based on which departments are selected"
    - "Review step shows all wizard selections with inline edit buttons per section"
    - "Full hierarchy is pre-selected by default when the wizard loads"
  artifacts:
    - path: "apps/web/_components/department-tree-select.tsx"
      provides: "Hover tooltip on tree nodes with role/budget/model details, hierarchy enforcement"
      contains: "tooltip"
    - path: "apps/web/_components/create-business-wizard.tsx"
      provides: "Dynamic provider list, inline review editing, correct redirect target"
      contains: "requiredProviders"
  key_links:
    - from: "apps/web/_components/create-business-wizard.tsx"
      to: "apps/web/_components/department-tree-select.tsx"
      via: "selectedTemplates state and DepartmentTemplate type"
      pattern: "selectedTemplates"
    - from: "apps/web/_components/create-business-wizard.tsx"
      to: "apps/web/_components/wizard-api-keys-step.tsx"
      via: "requiredProviders prop derived from selected templates"
      pattern: "requiredProviders"
---

<objective>
Polish the wizard UX to match user decisions from 18-CONTEXT.md: hover tooltips on department tree nodes (instead of inline details), dynamic API key provider list based on selected departments, inline editing on the review step, and hierarchy enforcement rules.

Purpose: The department tree currently shows description inline on each node. The user decided tree nodes should show name only with details on hover. The API key providers are static but should adapt to what agents are selected. The review step is view-only but should allow inline edits.

Output: Updated department-tree-select.tsx with hover tooltips and hierarchy enforcement, updated create-business-wizard.tsx with dynamic provider derivation, inline review editing, and correct step flow.
</objective>

<execution_context>
@/Users/tjbrooks/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tjbrooks/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-enhanced-business-wizard-agent-hierarchy/18-CONTEXT.md
@.planning/phases/18-enhanced-business-wizard-agent-hierarchy/18-RESEARCH.md
@.planning/phases/18-enhanced-business-wizard-agent-hierarchy/18-01-SUMMARY.md
@apps/web/_components/department-tree-select.tsx
@apps/web/_components/create-business-wizard.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add hover tooltips to department tree and enforce hierarchy rules</name>
  <files>
    apps/web/_components/department-tree-select.tsx
  </files>
  <action>
    **Update `department-tree-select.tsx` to show details on hover instead of inline:**

    1. **Move node details to a hover tooltip/popover:**
       - Each tree node currently shows name + description inline. Change to show name only (and role-level color badge) in the tree row.
       - On hover (or on a small info icon), show a tooltip/popover containing:
         - **Role:** e.g., "C-Suite (Level 0)", "Department Head (Level 1)", "Specialist (Level 2)"
         - **Token Budget:** e.g., "500K tokens/month"
         - **Model:** from template's model_profile (e.g., "Claude Opus 4", "Claude Sonnet 4")
         - **Description:** the full template description
       - Use CSS-based tooltip (title attribute is too basic). Implement with a `group` + `group-hover:visible` pattern on an absolutely positioned div, or use the existing shadcn Tooltip component if available (`@/components/ui/tooltip`). If Tooltip is not installed, use a simple CSS approach:
         ```
         <div className="group relative">
           <div>Node name</div>
           <div className="invisible group-hover:visible absolute left-full ml-2 top-0 z-50 w-64 rounded-md border bg-popover p-3 text-sm shadow-md">
             <p className="font-medium">{role}</p>
             <p className="text-muted-foreground">{description}</p>
             <p>Budget: {tokenBudget}</p>
           </div>
         </div>
         ```
       - The popover should appear to the right of the node on desktop. Add `pointer-events-none` to prevent it from interfering with selection clicks.

    2. **Add selection count summary:**
       - Below the tree, show: "{selectedCount} of {totalCount} agents selected" with a subtle text-muted-foreground style.
       - This replaces the need for a complex budget summary (user decision: "No token budget summary needed -- individual budgets visible on hover is enough").

    3. **Verify hierarchy enforcement is solid:**
       - CEO (executive department, roleLevel 0) must always be selected and checkbox disabled (already implemented -- verify it works)
       - Deselecting a department head (roleLevel 1) must auto-deselect all children (roleLevel 2) under that department (already implemented via cascade logic -- verify)
       - Selecting a specialist should NOT auto-select its parent head (user can manually select heads) -- verify current behavior

    4. **Keep expand/collapse working:** The existing expand/collapse per department should remain. Default state should be all departments expanded so user sees full hierarchy.

    **Patterns:**
    - group/group-hover for CSS-based tooltips (no extra JS library)
    - Keep the existing role-level color coding (amber for C-suite, blue for heads, slate for specialists)
    - Do NOT add any new npm dependencies for tooltips
  </action>
  <verify>
    Run `cd /Users/tjbrooks/Documents/Projects/Agency\ Factory && pnpm turbo build --filter=@agency-factory/web` to verify TypeScript compilation. Visually inspect that tree nodes show name only and details appear on hover.
  </verify>
  <done>
    Tree nodes display name and role badge only. Hover tooltip shows description, token budget, and model info. Selection count summary shown below tree. CEO always selected. Department head deselection cascades to children.
  </done>
</task>

<task type="auto">
  <name>Task 2: Dynamic provider list, inline review editing, and wizard flow polish</name>
  <files>
    apps/web/_components/create-business-wizard.tsx
  </files>
  <action>
    **Update `create-business-wizard.tsx` with three improvements:**

    1. **Dynamic API key provider list based on selected departments:**
       - After the Departments step (step 2), derive which providers are needed from the selected templates' `model_profile` fields.
       - The DEPARTMENT_TEMPLATES array already has model info. Add a helper function `deriveRequiredProviders(selectedTemplateIds: Set<string>, templates: DepartmentTemplate[])` that:
         - Anthropic is ALWAYS required (all primary agents use Claude)
         - If any R&D department templates are selected (departmentType === "rd"), add OpenAI, Google, Mistral, DeepSeek as "recommended" (R&D Council needs multi-model)
         - Return `{ id, name, description, required: boolean }[]` where `required` means the key must be provided
       - Pass this derived provider list as a `requiredProviders` prop to `WizardApiKeysStep`
       - Update `WizardApiKeysStep` to accept and use this prop instead of its hardcoded `API_KEY_PROVIDERS` array
       - Contextual help text per key: "Powers your Sales and Support agents" for Anthropic, "Required for R&D Council debates" for multi-model providers

    2. **Inline editing on the review step (step 5):**
       - The review step currently shows a read-only summary. Add "Edit" buttons per section:
         - **Business Details section:** "Edit" button that navigates back to step 1 (setCurrentStep(0))
         - **Departments section:** "Edit" button that navigates to step 2 (setCurrentStep(1))
         - **API Keys section:** "Edit" button that navigates to step 3 (setCurrentStep(2))
         - **Subdomain section:** "Edit" button that navigates to step 4 (setCurrentStep(3))
       - Each section in the review shows a summary card with the Edit button in the card header.
       - Use a small pencil icon (or text "Edit") styled as a ghost button in the top-right of each card.
       - After editing and advancing back through steps, the review step re-renders with updated data.

    3. **Pre-selected hierarchy defaults:**
       - Verify that `DEFAULT_SELECTED` set includes CEO + all 4 department heads + key specialists as documented.
       - If the current default is different from the full hierarchy (user decision: "Full hierarchy pre-selected by default"), update it to select ALL templates by default. Admin deselects what they don't need.

    **Patterns:**
    - useState for step navigation (already in place)
    - Ghost button variant for Edit buttons: `<Button variant="ghost" size="sm">`
    - Derive state from existing state (requiredProviders derived from selectedTemplates) rather than storing separately
  </action>
  <verify>
    Run `cd /Users/tjbrooks/Documents/Projects/Agency\ Factory && pnpm turbo build --filter=@agency-factory/web` to verify TypeScript compilation.
  </verify>
  <done>
    API key providers dynamically update when departments change (R&D selection adds multi-model keys). Review step has Edit buttons per section that navigate back to the correct wizard step. Full hierarchy pre-selected by default.
  </done>
</task>

</tasks>

<verification>
1. `pnpm turbo build --filter=@agency-factory/web` passes with no TypeScript errors
2. Department tree nodes show name + role badge only; hovering shows description, budget, model in a tooltip
3. Selection count "{N} of {M} agents selected" displays below the tree
4. Deselecting a department head auto-deselects all its specialists
5. CEO node checkbox is disabled and always checked
6. Selecting R&D department adds OpenAI/Google/Mistral/DeepSeek to the API keys step
7. Deselecting R&D department removes those providers from API keys step (if they were only needed for R&D)
8. Review step shows Edit buttons per section that navigate to the correct step
9. All templates are selected by default when wizard loads
</verification>

<success_criteria>
- Department tree UX matches user decision: name only in tree, details on hover
- Hierarchy enforcement is correct: CEO locked, cascade deselection works
- API key providers adapt to department selection (especially R&D triggering multi-model)
- Review step allows inline section editing via Edit buttons
- Full hierarchy pre-selected by default
</success_criteria>

<output>
After completion, create `.planning/phases/18-enhanced-business-wizard-agent-hierarchy/18-02-SUMMARY.md`
</output>
