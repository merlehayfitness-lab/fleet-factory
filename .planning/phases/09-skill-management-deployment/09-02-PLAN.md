---
phase: 09-skill-management-deployment
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - apps/web/_actions/skill-actions.ts
  - apps/web/_components/skill-editor.tsx
  - apps/web/_components/skill-assignment-list.tsx
  - apps/web/_components/skill-usage-card.tsx
  - apps/web/_components/agent-skills-tab.tsx
  - apps/web/_components/agent-detail-tabs.tsx
  - apps/web/_components/agent-card.tsx
  - apps/web/_components/agents-list.tsx
  - apps/web/app/(dashboard)/businesses/[id]/agents/[agentId]/page.tsx
  - apps/web/app/(dashboard)/businesses/[id]/agents/page.tsx
autonomous: true
requirements:
  - SKILL-01
  - SKILL-03

must_haves:
  truths:
    - "Server Actions exist for skill CRUD: createSkillAction, updateSkillAction, deleteSkillAction, assignSkillAction, unassignSkillAction"
    - "Split-pane skill editor opens in a Dialog: structured form (name, description, instructions, trigger phrases) on left, live SKILL.md preview on right"
    - "Creating a new skill offers 'Start from template' option before showing blank editor"
    - "Each save increments the version number displayed in the editor"
    - "Skills are always live once saved -- no draft/active toggle"
    - "Skill assignment list shows checkboxes: checked = assigned, unchecked = not assigned"
    - "Department-inherited skills show 'Inherited' badge and cannot be unchecked at agent level"
    - "Unassigning a skill shows confirmation dialog"
    - "Skill usage card shows 'Used by N agents, N departments' with expandable list of names"
    - "New 'Skills' tab on agent detail page between Integrations and Knowledge tabs"
    - "Agent cards on agents list page show skill count badge (e.g., '3 skills')"
    - "Agent detail page fetches skill assignments and passes to Skills tab"
  artifacts:
    - path: "apps/web/_actions/skill-actions.ts"
      provides: "Server Actions for skill CRUD and assignment"
      exports: ["createSkillAction", "updateSkillAction", "deleteSkillAction", "assignSkillAction", "unassignSkillAction", "getSkillsForAgentAction", "getSkillTemplatesAction", "createSkillFromTemplateAction"]
    - path: "apps/web/_components/skill-editor.tsx"
      provides: "Split-pane skill editor dialog with form and preview"
      contains: "SkillEditor"
    - path: "apps/web/_components/skill-assignment-list.tsx"
      provides: "Checkbox list for assigning/unassigning skills to an agent"
      contains: "SkillAssignmentList"
    - path: "apps/web/_components/skill-usage-card.tsx"
      provides: "Usage stats card with expandable agent/department list"
      contains: "SkillUsageCard"
    - path: "apps/web/_components/agent-skills-tab.tsx"
      provides: "Skills tab content for agent detail page"
      contains: "AgentSkillsTab"
  key_links:
    - from: "apps/web/_actions/skill-actions.ts"
      to: "packages/core/skill/skill-service.ts"
      via: "Server Actions call core skill service functions"
      pattern: "createSkill|updateSkill|assignSkill|unassignSkill"
    - from: "apps/web/_components/skill-editor.tsx"
      to: "apps/web/_actions/skill-actions.ts"
      via: "Editor save calls createSkillAction or updateSkillAction"
      pattern: "createSkillAction|updateSkillAction"
    - from: "apps/web/_components/skill-assignment-list.tsx"
      to: "apps/web/_actions/skill-actions.ts"
      via: "Checkbox toggle calls assignSkillAction or unassignSkillAction"
      pattern: "assignSkillAction|unassignSkillAction"
    - from: "apps/web/_components/agent-detail-tabs.tsx"
      to: "apps/web/_components/agent-skills-tab.tsx"
      via: "New Skills tab renders AgentSkillsTab component"
      pattern: "AgentSkillsTab"
---

<objective>
Build the skill editor UI, skill assignment list, skill usage card, Skills tab on agent detail, and skill count badge on agent cards.

Purpose: This plan creates the agent-level skill management UI. The split-pane skill editor (form left, live preview right) enables creating and editing SKILL.md files with structured sections. The assignment list with checkboxes enables assigning/unassigning skills to agents, with inherited department-level skills shown as non-removable with an "Inherited" badge. A new Skills tab on the agent detail page houses these components. Agent cards on the agents list page gain a skill count badge.

Output: Server Actions file, 4 new UI components, updated agent detail tabs, updated agent card and agents list, updated agent detail and agents list pages.
</objective>

<execution_context>
@/Users/tjbrooks/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tjbrooks/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-skill-management-deployment/09-CONTEXT.md
@.planning/phases/09-skill-management-deployment/09-RESEARCH.md
@.planning/phases/09-skill-management-deployment/09-01-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Server Actions and skill editor component</name>
  <files>
    apps/web/_actions/skill-actions.ts
    apps/web/_components/skill-editor.tsx
    apps/web/_components/skill-usage-card.tsx
  </files>
  <action>
    **apps/web/_actions/skill-actions.ts:**
    "use server" module with Server Actions for skill management:

    - `createSkillAction(businessId, data: {name, description?, content, trigger_phrases?})` -- auth check, call createSkill from core, revalidatePath, return {skill} or {error}
    - `updateSkillAction(skillId, businessId, data: {name?, description?, content?, trigger_phrases?})` -- auth check, call updateSkill (increments version), revalidatePath, return {skill} or {error}
    - `deleteSkillAction(skillId, businessId)` -- auth check, call softDeleteSkill, revalidatePath, return {success} or {error}
    - `assignSkillAction(skillId, businessId, target: {agent_id?, department_id?})` -- auth check, call assignSkill, revalidatePath, return {assignment} or {error}
    - `unassignSkillAction(assignmentId, businessId)` -- auth check, call unassignSkill, revalidatePath, return {success} or {error}
    - `getSkillsForAgentAction(agentId, departmentId)` -- auth check, call getSkillsForAgent, return SkillWithAssignment[] or {error}
    - `getSkillTemplatesAction(departmentType?)` -- auth check, call getSkillTemplates, return SkillTemplate[]
    - `createSkillFromTemplateAction(businessId, templateId)` -- auth check, call createSkillFromTemplate, revalidatePath, return {skill} or {error}
    - `getSkillUsageAction(skillId)` -- auth check, call getSkillUsage, return SkillUsage

    **apps/web/_components/skill-editor.tsx:**
    "use client" Dialog-based split-pane editor.
    Props: `businessId`, `skill` (Skill | null -- null for new skill), `onSave` callback, `onClose` callback.

    Layout inside a Dialog (full-width, max-w-5xl):
    - Header: "Edit Skill" or "New Skill" with version badge ("v{version}") for existing skills
    - Two-column grid (md:grid-cols-2, stacked on mobile):
      - Left pane (form):
        - Name: text Input (required)
        - Description: Textarea (2-3 rows)
        - Instructions: large Textarea (8+ rows, monospace) -- this is the main skill content
        - Trigger Phrases: text Input with helper text "Comma-separated phrases that activate this skill"
      - Right pane (preview):
        - Header: "Preview" with muted text
        - Live-updating SKILL.md preview in a pre/code block with monospace font
        - Preview compiles the form fields into SKILL.md format matching the frontmatter + section pattern:
          ```
          ---
          name: {name}
          description: {description}
          triggers: {comma-separated triggers}
          ---

          # {name}

          {instructions content}
          ```
    - Footer: Save and Cancel buttons. Save calls createSkillAction (new) or updateSkillAction (existing), then onSave callback. Cancel calls onClose.
    - Loading state on Save button during submission.

    State: all form fields in React state, initialized from skill prop (or empty for new). Preview computed from state on each render (no debounce needed, just string concatenation).

    **apps/web/_components/skill-usage-card.tsx:**
    "use client" component showing skill usage statistics.
    Props: `skillId`, `businessId`.

    On mount, fetch usage via getSkillUsageAction. Display:
    - Summary line: "Used by {N} agents, {N} departments"
    - Expandable section (useState toggle) showing agent names and department names as lists
    - Loading skeleton while fetching
    - Empty state if no assignments

    Run `pnpm turbo typecheck` to verify.
  </action>
  <verify>
    Run `pnpm turbo typecheck` (no type errors).
    Verify skill-actions.ts exports all 9 Server Actions with auth checks.
    Verify skill-editor.tsx renders split-pane Dialog with form left and preview right.
    Verify preview updates live from form state without server round-trip.
    Verify Save calls createSkillAction for new skills and updateSkillAction for existing.
    Verify skill-usage-card.tsx fetches and displays usage stats with expandable list.
  </verify>
  <done>
    Server Actions for skill CRUD and assignment. Split-pane skill editor Dialog with structured form (name, description, instructions, triggers) on left and live SKILL.md preview on right. Skill usage card with expandable agent/department list.
  </done>
</task>

<task type="auto">
  <name>Task 2: Skill assignment list, Skills tab, agent card badge, and page updates</name>
  <files>
    apps/web/_components/skill-assignment-list.tsx
    apps/web/_components/agent-skills-tab.tsx
    apps/web/_components/agent-detail-tabs.tsx
    apps/web/_components/agent-card.tsx
    apps/web/_components/agents-list.tsx
    apps/web/app/(dashboard)/businesses/[id]/agents/[agentId]/page.tsx
    apps/web/app/(dashboard)/businesses/[id]/agents/page.tsx
  </files>
  <action>
    **apps/web/_components/skill-assignment-list.tsx:**
    "use client" component with checkbox list for agent skill assignment.
    Props: `businessId`, `agentId`, `departmentId`, `initialSkills` (SkillWithAssignment[]), `allBusinessSkills` (Skill[]).

    Layout:
    - List of all business skills with checkboxes
    - Checked state: skill is assigned to this agent (either directly or via department)
    - Skills with assignment_level='department' show "Inherited" Badge and checkbox is disabled (cannot uncheck)
    - Skills with assignment_level='agent' can be unchecked to unassign
    - Unchecking triggers a confirmation Dialog: "Remove {skill.name} from this agent?"
    - On confirm: call unassignSkillAction with the assignment_id
    - Checking an unassigned skill: call assignSkillAction with agent_id
    - Toast notifications for success/error via sonner
    - Use native HTML checkbox (matching Phase 4 pattern -- no shadcn Checkbox installed)
    - Empty state if no business skills exist: "No skills in library. Create one to get started."

    **apps/web/_components/agent-skills-tab.tsx:**
    "use client" component for the Skills tab content.
    Props: `businessId`, `agentId`, `departmentId`, `agentName`.

    Layout:
    - Header row with title "Skills" and action buttons:
      - "New Skill" button -- opens SkillEditor in create mode
      - "Add from Templates" button -- placeholder for 09-03 (disabled with tooltip "Coming in next update" or just shows template browser if 09-03 exists)
      - "Import from GitHub" button -- placeholder for 09-03 (same treatment)
    - Skill assignment list below the header (fetches skills on mount via getSkillsForAgentAction)
    - Click on a skill name opens SkillEditor in edit mode
    - Each skill row shows: checkbox, name, description (truncated), source badge (manual/imported/template), version badge, and "Inherited" badge if department-level
    - SkillUsageCard shown inline when a skill is selected/expanded
    - Loading skeleton while fetching skills

    State management: skills array in React state, updated after create/assign/unassign operations via refetch.

    **apps/web/_components/agent-detail-tabs.tsx:**
    Add "Skills" tab to the existing 6-tab layout:
    1. Import AgentSkillsTab from agent-skills-tab
    2. Add TabsTrigger for "skills" between "integrations" and "knowledge" (7th tab total)
    3. Add TabsContent rendering AgentSkillsTab with businessId, agent.id, agent.departments?.id, agent.name

    **apps/web/_components/agent-card.tsx:**
    Add optional `skillCount` prop to AgentCardProps:
    ```typescript
    interface AgentCardProps {
      agent: { /* existing */ };
      businessId: string;
      role?: string | null;
      skillCount?: number;
    }
    ```

    In CardContent, after the template and model info, add skill count display:
    ```tsx
    {typeof skillCount === 'number' && skillCount > 0 && (
      <p className="text-xs text-muted-foreground">
        {skillCount} {skillCount === 1 ? 'skill' : 'skills'}
      </p>
    )}
    ```

    **apps/web/_components/agents-list.tsx:**
    Update Agent interface to include optional `skill_count` field (number | undefined).
    Pass `skillCount={agent.skill_count}` to each AgentCard.

    **apps/web/app/(dashboard)/businesses/[id]/agents/[agentId]/page.tsx:**
    Agent detail page updates:
    - The existing agent query should already fetch all needed fields
    - No additional skill data fetch needed at page level -- AgentSkillsTab fetches its own data client-side via Server Actions
    - Ensure businessId is passed through to AgentDetailTabs (already done)

    **apps/web/app/(dashboard)/businesses/[id]/agents/page.tsx:**
    Agents list page updates:
    - After fetching agents, query skill counts per agent. Use a separate query:
      ```sql
      SELECT agent_id, COUNT(*) as count
      FROM skill_assignments
      WHERE business_id = ? AND agent_id IS NOT NULL
      GROUP BY agent_id
      ```
    - Also count department-level skills per department and add to agents in that department
    - Merge counts into agent data, passing total (direct + inherited) as skill_count
    - Pass to AgentsList component

    Run `pnpm turbo typecheck` to verify.
  </action>
  <verify>
    Run `pnpm turbo typecheck` (no type errors).
    Verify skill-assignment-list.tsx shows checkboxes with disabled "Inherited" badge for department skills.
    Verify unassign shows confirmation dialog before removing.
    Verify agent-skills-tab.tsx has "New Skill", "Add from Templates", "Import from GitHub" buttons.
    Verify clicking a skill opens the SkillEditor in edit mode.
    Verify agent-detail-tabs.tsx has 7 tabs with Skills between Integrations and Knowledge.
    Verify agent-card.tsx shows skill count when skillCount prop is provided.
    Verify agents-list.tsx passes skill_count to AgentCard.
    Verify agents page queries and merges skill counts into agent data.
  </verify>
  <done>
    Skill assignment list with checkboxes (inherited badge for department-level, confirmation on unassign). Agent Skills tab with New Skill, Add from Templates, and Import from GitHub buttons. Skill editor opens on click. 7-tab agent detail layout. Skill count badge on agent cards. Agents list page queries and displays skill counts.
  </done>
</task>

</tasks>

<verification>
- `pnpm turbo typecheck` passes
- Server Actions handle skill CRUD with auth checks, revalidation, and error handling
- Split-pane skill editor Dialog shows form on left and live SKILL.md preview on right
- Creating new skill from editor calls createSkillAction, editing calls updateSkillAction with version increment
- Skill assignment list shows all business skills with checked/unchecked state per agent
- Department-inherited skills show "Inherited" badge with disabled checkbox
- Unassigning a skill requires confirmation dialog
- Skills tab on agent detail page between Integrations and Knowledge
- "New Skill" button opens editor in create mode
- Clicking existing skill opens editor in edit mode
- Skill usage card shows agent/department count with expandable names
- Agent cards on agents list show skill count badge
- Agents list page queries skill_assignments for per-agent counts
</verification>

<success_criteria>
Skill editor UI allows creating and editing SKILL.md files with structured sections via split-pane Dialog (SKILL-01). Department-level skills displayed with "Inherited" badge on agent Skills tab, cannot be removed at agent level (SKILL-03 UI). Skill assignment checkboxes enable direct agent-level skill management. Agent cards show skill counts.
</success_criteria>

<output>
After completion, create `.planning/phases/09-skill-management-deployment/09-02-SUMMARY.md`
</output>
