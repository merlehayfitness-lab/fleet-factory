---
phase: 09-skill-management-deployment
plan: 03
type: execute
wave: 3
depends_on:
  - 09-01
  - 09-02
files_modified:
  - apps/web/_actions/skill-actions.ts
  - apps/web/_components/skill-template-browser.tsx
  - apps/web/_components/github-import-dialog.tsx
  - apps/web/_components/skill-library.tsx
  - apps/web/_components/department-skills-panel.tsx
  - apps/web/_components/agent-skills-tab.tsx
  - apps/web/_components/sidebar-nav.tsx
  - apps/web/app/(dashboard)/businesses/[id]/skills/page.tsx
  - apps/web/app/(dashboard)/businesses/[id]/departments/page.tsx
autonomous: true
requirements:
  - SKILL-02
  - SKILL-03
  - SKILL-04

must_haves:
  truths:
    - "Skill template browser shows card grid with name, description, and department/role tags"
    - "Templates filterable by department type (Sales, Support, Operations, Owner) and role type"
    - "Template preview shows full content before adding to business library"
    - "Adding a template creates a copy in business library with source_type='template' (copy-and-customize model)"
    - "Template browser accessible from agent Skills tab ('Add from Templates' button) and standalone library page"
    - "GitHub import dialog accepts URL input, detects file vs directory, shows preview for single files"
    - "Directory imports fetch all .md files and create skills in batch"
    - "Imported skills track source_url for future re-sync"
    - "Imported skills auto-assign to current agent AND add to business skill library"
    - "Standalone skill library page at /businesses/[id]/skills shows all business skills in card grid"
    - "Skill library page supports filtering, editing, deletion, and usage viewing"
    - "Sidebar nav has 'Skills' item linking to /businesses/[id]/skills"
    - "Department-level skill assignment available from departments page"
    - "Department skills panel shows assigned skills with add/remove capability"
  artifacts:
    - path: "apps/web/_components/skill-template-browser.tsx"
      provides: "Card grid template browser with department/role filters and preview"
      contains: "SkillTemplateBrowser"
    - path: "apps/web/_components/github-import-dialog.tsx"
      provides: "GitHub URL import dialog with preview and batch import"
      contains: "GitHubImportDialog"
    - path: "apps/web/_components/skill-library.tsx"
      provides: "Business skill library card grid with filters and actions"
      contains: "SkillLibrary"
    - path: "apps/web/_components/department-skills-panel.tsx"
      provides: "Department-level skill assignment panel"
      contains: "DepartmentSkillsPanel"
    - path: "apps/web/app/(dashboard)/businesses/[id]/skills/page.tsx"
      provides: "Standalone skill library page route"
      contains: "SkillsPage"
  key_links:
    - from: "apps/web/_components/skill-template-browser.tsx"
      to: "apps/web/_actions/skill-actions.ts"
      via: "Add template calls createSkillFromTemplateAction then assignSkillAction"
      pattern: "createSkillFromTemplateAction"
    - from: "apps/web/_components/github-import-dialog.tsx"
      to: "apps/web/_actions/skill-actions.ts"
      via: "Import calls importFromGitHubAction which uses parseGitHubUrl + fetch + createSkill"
      pattern: "importFromGitHubAction"
    - from: "apps/web/_components/department-skills-panel.tsx"
      to: "apps/web/_actions/skill-actions.ts"
      via: "Assign/unassign department skills via assignSkillAction/unassignSkillAction with department_id"
      pattern: "assignSkillAction|unassignSkillAction"
---

<objective>
Build the skill template library browser, GitHub import dialog, standalone skill library page, department-level skill assignment, and sidebar navigation update.

Purpose: This plan creates the template browsing experience (card grid with department/role filters, preview, copy-and-customize), the GitHub import flow (URL input, file/directory detection, preview, batch import), the standalone skill library page for business-wide skill management, and department-level skill assignment. It also wires the "Add from Templates" and "Import from GitHub" buttons on the agent Skills tab created in 09-02, and adds the Skills nav item to the sidebar.

Output: Template browser, GitHub import dialog, skill library page and component, department skills panel, updated agent skills tab, updated sidebar nav, updated departments page.
</objective>

<execution_context>
@/Users/tjbrooks/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tjbrooks/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-skill-management-deployment/09-CONTEXT.md
@.planning/phases/09-skill-management-deployment/09-RESEARCH.md
@.planning/phases/09-skill-management-deployment/09-01-SUMMARY.md
@.planning/phases/09-skill-management-deployment/09-02-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Server Action additions, template browser, GitHub import dialog, and agent skills tab wiring</name>
  <files>
    apps/web/_actions/skill-actions.ts
    apps/web/_components/skill-template-browser.tsx
    apps/web/_components/github-import-dialog.tsx
    apps/web/_components/agent-skills-tab.tsx
  </files>
  <action>
    **apps/web/_actions/skill-actions.ts:**
    Add new Server Actions to the existing file:

    - `importFromGitHubAction(businessId, url: string, agentId?: string)` -- auth check, call parseGitHubUrl. If file: call fetchGitHubFile, create skill with source_type='imported' and source_url, optionally assign to agent. If directory: call fetchGitHubDirectory, create each skill, optionally assign all to agent. Return {skills: Skill[]} or {error}. Revalidate path.
    - `previewGitHubUrlAction(url: string)` -- auth check, call parseGitHubUrl to validate. If file: fetch content and return {type: 'file', preview: GitHubImportResult}. If directory: list files and return {type: 'directory', files: string[]}. Return {error} for invalid URLs. No database writes -- preview only.

    **apps/web/_components/skill-template-browser.tsx:**
    "use client" Dialog or panel component for browsing and selecting skill templates.
    Props: `businessId`, `agentId` (optional -- when provided, auto-assigns after adding), `onSkillAdded` callback.

    Layout inside a Dialog (max-w-4xl):
    - Header: "Skill Templates"
    - Filter bar: Department type Select (All, Owner, Sales, Support, Operations) and Role type Select (All, plus dynamic options from templates)
    - Card grid (2-3 columns): each card shows:
      - Template name (bold)
      - Description (truncated, 2 lines)
      - Department badge and role type badge
      - "Preview" button and "Add to Library" button
    - Preview: clicking Preview opens a nested panel/section showing full template content in monospace pre block
    - Add to Library: calls createSkillFromTemplateAction, then if agentId is provided, calls assignSkillAction. Shows toast on success. Calls onSkillAdded callback.
    - Loading state while fetching templates via getSkillTemplatesAction on mount
    - Empty state if no templates match filters

    State: department filter, role filter, selected preview template, all in React state. Templates fetched once on mount.

    **apps/web/_components/github-import-dialog.tsx:**
    "use client" Dialog for importing skills from GitHub.
    Props: `businessId`, `agentId` (optional), `onImported` callback.

    Layout inside a Dialog:
    - Header: "Import from GitHub"
    - URL Input with placeholder "https://github.com/owner/repo/blob/main/skills/my-skill.md"
    - "Check URL" button that calls previewGitHubUrlAction
    - After check:
      - File detected: show content preview in monospace pre block with skill name extracted from filename. "Import" button to proceed.
      - Directory detected: show list of .md files found with count. "Import All" button to proceed.
      - Invalid URL: show error message in red
    - Import flow:
      - Calls importFromGitHubAction with businessId, url, and optional agentId
      - Shows progress indicator during import
      - On success: toast with count of skills imported, calls onImported callback
      - On error: toast with error message
    - Info text at bottom: "Public repositories only. Imported skills are added to your business library."

    **apps/web/_components/agent-skills-tab.tsx:**
    Update the existing component from 09-02:
    - Wire "Add from Templates" button to open SkillTemplateBrowser Dialog with agentId
    - Wire "Import from GitHub" button to open GitHubImportDialog with agentId
    - Both dialogs pass onSkillAdded/onImported callbacks that refetch the skills list
    - Remove placeholder/disabled state from these buttons

    Run `pnpm turbo typecheck` to verify.
  </action>
  <verify>
    Run `pnpm turbo typecheck` (no type errors).
    Verify importFromGitHubAction handles both file and directory URLs.
    Verify previewGitHubUrlAction returns preview content for files and file list for directories.
    Verify skill-template-browser.tsx shows filterable card grid with preview and add-to-library.
    Verify template add creates a copy in business library and optionally assigns to agent.
    Verify github-import-dialog.tsx shows URL input, preview step, and import action.
    Verify directory import creates all .md files as skills in batch.
    Verify imported skills have source_type='imported' and source_url set.
    Verify agent-skills-tab.tsx "Add from Templates" and "Import from GitHub" buttons are wired.
  </verify>
  <done>
    Template browser with card grid, department/role filters, preview, and copy-and-customize add flow. GitHub import dialog with URL validation, file/directory detection, content preview, and batch import. Both integrated into agent Skills tab with auto-assign to current agent. Server Actions for import and preview operations.
  </done>
</task>

<task type="auto">
  <name>Task 2: Skill library page, department skills panel, sidebar nav, and departments page update</name>
  <files>
    apps/web/_components/skill-library.tsx
    apps/web/_components/department-skills-panel.tsx
    apps/web/_components/sidebar-nav.tsx
    apps/web/app/(dashboard)/businesses/[id]/skills/page.tsx
    apps/web/app/(dashboard)/businesses/[id]/departments/page.tsx
  </files>
  <action>
    **apps/web/_components/skill-library.tsx:**
    "use client" component for the business skill library.
    Props: `businessId`, `initialSkills` (Skill[]).

    Layout:
    - Header row: "Skill Library" title with "New Skill" button (opens SkillEditor), "Import from GitHub" button (opens GitHubImportDialog), "Browse Templates" button (opens SkillTemplateBrowser)
    - Filter/search bar: text Input for search by name, Select for source_type filter (All, Manual, Imported, Template)
    - Card grid (responsive: 1-3 columns):
      - Each card shows: skill name, description (truncated), source badge (Manual/Imported/Template with different colors), version badge, trigger phrases as small tags
      - Card actions: click to open SkillEditor, kebab menu with "View Usage" and "Delete" options
      - "View Usage" opens SkillUsageCard inline or in a popover
      - "Delete" shows confirmation dialog with warning: "This skill will be removed from the library. Agents with this skill assigned will keep their copy." On confirm: call deleteSkillAction (soft delete)
    - Empty state: "No skills yet. Create one, import from GitHub, or browse templates to get started."

    State: search query, source filter, skills list in React state. Filter client-side from initialSkills (or refetch after mutations).

    **apps/web/_components/department-skills-panel.tsx:**
    "use client" component for department-level skill assignment.
    Props: `businessId`, `departmentId`, `departmentName`.

    Layout:
    - Card with header: "Department Skills" with count badge
    - Description: "Skills assigned here are inherited by all agents in {departmentName}."
    - List of currently assigned department skills with unassign (X) button each
    - "Add Skill" button: opens a Select/dropdown of business skills not yet assigned to this department. On selection: call assignSkillAction with department_id.
    - Unassign: confirmation dialog, then call unassignSkillAction
    - Fetch department skills on mount via a Server Action (getSkillsForDepartment wrapped in a new action, or use existing getSkillsForAgentAction pattern)
    - Toast notifications for success/error

    Add a new Server Action to skill-actions.ts if needed:
    - `getDepartmentSkillsAction(departmentId)` -- call getSkillsForDepartment, return Skill[]

    **apps/web/_components/sidebar-nav.tsx:**
    Add "Skills" item to the business sub-nav in `getBusinessSubNav`:
    ```typescript
    {
      href: `/businesses/${businessId}/skills`,
      label: "Skills",
      icon: Sparkles, // or Wand2 or Layers from lucide-react
      enabled: true,
    },
    ```
    Place it after "Templates" and before "Deployments" in the nav order. Import the chosen icon from lucide-react.

    **apps/web/app/(dashboard)/businesses/[id]/skills/page.tsx:**
    Server Component for the standalone skill library page:
    - Auth check, redirect if not authenticated
    - Fetch business (notFound if missing)
    - Fetch all skills for business via listSkillsForBusiness (using createSupabaseServerClient)
    - Render page header: "Skills" with business name breadcrumb
    - Render SkillLibrary component with businessId and initialSkills

    **apps/web/app/(dashboard)/businesses/[id]/departments/page.tsx:**
    Update the existing departments page:
    - For each department card/section, add the DepartmentSkillsPanel component below the existing department info
    - Pass businessId, department.id, and department.name to each panel
    - The panel fetches its own skills data client-side, so no additional server queries needed at page level

    Run `pnpm turbo typecheck` to verify.
  </action>
  <verify>
    Run `pnpm turbo typecheck` (no type errors).
    Verify skill-library.tsx shows card grid with search, source filter, and CRUD actions.
    Verify delete shows warning about agents keeping their copy, then soft-deletes.
    Verify department-skills-panel.tsx shows assigned skills with add/remove capability.
    Verify department assignment uses department_id in assignSkillAction.
    Verify sidebar-nav.tsx has "Skills" item after Templates in business sub-nav.
    Verify /businesses/[id]/skills page renders SkillLibrary with fetched skills.
    Verify departments page includes DepartmentSkillsPanel for each department.
  </verify>
  <done>
    Standalone skill library page at /businesses/[id]/skills with card grid, search/filter, and CRUD actions. Department-level skill assignment panel on departments page with add/remove and inheritance description. Sidebar nav updated with Skills item. Soft delete warns about agent retention. Template browser, GitHub import, and new skill creation accessible from library page.
  </done>
</task>

</tasks>

<verification>
- `pnpm turbo typecheck` passes
- Skill template browser shows card grid filterable by department and role type
- Template preview shows full content before adding to business library
- Adding template creates a copy (source_type='template') in business library
- Template browser accessible from agent Skills tab and standalone library page
- GitHub import dialog accepts URL, detects file vs directory, shows preview
- Single file import creates one skill with source_url tracked
- Directory import creates skills for all .md files in batch
- Imported skills auto-assign to current agent (when from agent Skills tab)
- Standalone skill library page at /businesses/[id]/skills shows all business skills
- Library supports search, source filter, edit (opens editor), delete (soft delete with warning), and usage viewing
- Sidebar nav has "Skills" item linking to /businesses/[id]/skills
- Department-level skill assignment on departments page with add/remove
- Department skills panel describes inheritance: "inherited by all agents in this department"
</verification>

<success_criteria>
Skills importable from GitHub repository URLs with file and directory support, preview before import, source URL tracking (SKILL-02). Department-level skills assignable from departments page, inherited by all agents with add/remove (SKILL-03). Skill template library with curated cards, department/role filters, preview, and copy-and-customize model accessible from two paths (SKILL-04).
</success_criteria>

<output>
After completion, create `.planning/phases/09-skill-management-deployment/09-03-SUMMARY.md`
</output>
